node_modules/
.next/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Claude Code local settings (may contain approved commands with secrets)
.claude/settings.local.json

.vercel
.env*.local

# ── Never commit service-account credentials or other secret files ──
# Google Cloud service account JSON keys (downloaded from Console → Keys → Create)
**/*-iam.gserviceaccount.com.json
**/global-master-list-hris-*.json
**/service-account-*.json
**/*-service-account.json
**/credentials*.json
# Safe escape hatch — anything dropped in references/secrets/ is ignored.
references/secrets/
# Local data-fix backups (full employee_ids rows incl. bank numbers) — never commit.
references/backups/

# Local git worktrees for isolated feature work.
.worktrees/

# ── Payroll sheet exports — real names, addresses, bank details and salaries ──
# Exported from the Google Sheets payroll dashboards to verify our engine against the
# authority Accounting pays from (scripts/verify-hogan-formula.mts reads one of these).
# Useful locally, must never enter git history.
*Payroll Dashboard*.csv
*Payroll Dashboard*.xlsx
NEW Payroll Dashboard*.csv
