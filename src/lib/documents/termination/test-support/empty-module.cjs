/* [TERMINATION-DOCS] TEST SUPPORT — the empty module `server-only` resolves to
 * inside a test process. Next supplies exactly this alias for its server
 * bundles; there is no `server-only` package to install. See
 * ./stub-server-modules.ts. */
module.exports = {};
