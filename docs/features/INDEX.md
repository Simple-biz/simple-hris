# Feature doc index

Surface → governing docs → memory entries → the rule most likely to be violated.

Used by the `hardening` skill as step 1. **Look up here before editing code.**
A surface with no row is not proof there is no doc — grep `docs/` and the memory directory first, then say `READ none` out loud.

**Maintenance:** a new doc in `docs/features/` means a new row, same commit.
A memory entry that corrects a doc means the `Key invariant` cell gets corrected too.
A new memory entry means a new wikilink in the matching row's `Memory` cell, same commit — a mapped-but-incomplete row hides entries worse than a missing row does.

| Surface | Docs | Memory | Key invariant |
|---|---|---|---|
| Payment dispatch & bank routing | `payment-dispatch.md` · `bank-preferred-routing.md` (§10 KPI band) · `cop-country-payees.md` | [[bank-preferred-is-routing-do-not-seed]] · [[bank-preferred-field]] · [[bank-preferred-approval-gate]] · [[wires-lock-shipped]] · [[small-wires-wise-reroute]] · [[hurupay-no-email-wires-flip]] · [[mark-paid-bank-override]] · [[people-tab-bank-drift-audit]] · [[cop-country-payees-dispatch]] · [[contractor-invoice-payment-toggle]] · [[ph-freelancers-wise-seed]] · [[wise-employee-pickers-unretired]] · [[hsl-npd-bank-list-seed]] · [[nobank-list-clobbered-submissions]] · [[bank-info-notify-webhook]] · [[bank-info-temporary-exemption]] · [[gracea-wrong-bank-seeded]] · [[payment-dispatch-master-cap-and-staged-safety-net]] · [[dispatch-wizard-values-precedence]] · [[dispatch-log-department-filter]] · [[people-rail-mix-kpi-band]] · [[hurupay-kolan-rebrand]] · [[wallet-rail-mirror-and-lock]] | **Hurupay rebranded to Kolan 2026-08-24 — LABEL ONLY.** The processor id, `employee_ids.bank_preferred`, the `payment_method` CHECK and the `hurupay_email` / `"Hurupay Email"` columns all still read `hurupay`; `kolan` is accepted as an alias by all three normalisers and by `isWiresPreferred` so a sheet cell typed "Kolan" cannot resolve to null (unrouted ⇒ dropped from the dispatch queue ⇒ silently unpaid) or read as WIRES. **The two WALLET rails are coupled one-way (2026-08-24):** setting Bank Preferred to Kolan/HiGlobe also sets `preferred_processor` (`mirroredDisbursementFor`, applied server-side) — wise/jeeves/wires impose nothing, and **the receiving account is never written by a rail pick**. **`null` is no longer a wallet lockout, but "unset" means UNROUTED IN ALL THREE TIERS:** `isWiresPreferred` (routing — unset ⇒ wires) and `isWalletRailLocked` (the guard — unset ⇒ assignable) are separate predicates, and the guard MUST be fed the **effective** rail via `resolveWalletRailLock()`, which resolves `bank_preferred` → `preferred_processor` → the legacy rates cell and **fails closed**. Reading tier 1 alone lets the ~1,351 legacy-cell-seeded wires payees (tier 1 still NULL) onto a wallet in one save. Narrowing what `null` means also silently flipped the approval PATCH from fail-closed to fail-OPEN — re-audit every read-error-falls-to-null site whenever that meaning changes. **"Bank Preferred" is the SEND-FROM rail** (which processor Accounting pays out on) — a different thing from the employee's RECEIVING account. Never seed one from the other. `wires`/null never auto-flip to hurupay/higlobe. **The amount and its itemization both come from the WIZARD, via one shared rule** (`wizard-dispatch-values.ts`): the published snapshot only when it out-dates `locked_at` + is itemized + catalog-consistent + the row isn't held, else the LOCKED stage, else a recompute that must SAY so — never a wizard total beside a recomputed split. Measured clean on the 2026-08-16 cycle (1,040/1,040 wizard-priced, 0 recomputed) by `scripts/verify-dispatch-carryover.mts`, which is the tool to run before believing any "dispatch doesn't match the wizard" report. **The EXPORTS are the sheet-validation artifact, so two identities are load-bearing** (§4.2.3, `dispatch-client-csv.test.ts`): `Regular+OT + Bonus Total + Orphanage − MESA Ded + MESA Disb = Amount (PHP)` **and** `PAB + Tech + Other Bonuses + Adjustment = Bonus Total`. The second was unprovable from the file until 2026-08-25 — the pending CSV had no Other-Bonuses and no Adjustment column, so a signed **withholding** hid inside an aggregate (694 rows / ₱1.83M other, 86 Adj. of which 6 negative). Both carriers already held the fields; they were dropped at the breakdown boundary, so `WizardBreakdown` and `QueueRow` now carry them and the compiler flags every construction site. **And nothing a screen shows may vanish from that screen's export** — the log views' **COP Value** and **System Bonus** columns were absent from every sent/paid/done CSV, hiding ₱5,519,915 of frozen bonus across 1,606 records. Live sync between open screens is Realtime **Broadcast**, never `postgres_changes` (anon + RLS ⇒ row events never reach the browser). **A blank queue is not a finished payroll** (OPEN, 2026-08-18 §12.7.1): `hydrated` resets only when `loading` flips true, and every live path (`lock flip / broadcast / 15s poll / focus`) loads `{silent:true}`, which never sets `loading` — so `pending` can render stale-empty beside a fresh `paid` row and report 100%. Any new "is the cycle done?" read must cross-check `cycleStartedCount`, and `payJson.error` still goes uninspected. **People → Bank changes opens with two KPI cards** (2026-08-19, `bank-preferred-routing.md` §10), and **both are rail-shaped with identical rows**: *Preferred bank · send-from* counts who each rail carries; *Receiving details on file* counts how many of those are payable there (`isPayoutComplete`), with the requirement as the row caption. **There are NO bank names on this band** and none may be reintroduced: `employee_ids.bank_name` is free text carrying ~100 spellings of ~30 banks (three for BPI alone), and a bank is not the unit anyone pays on — GoTyme measured `wise 132 · wires 45`. Normalizing that column is a script with an `--apply` gate, never a display trick. Both cards fold from `PeopleSummary.railMix`, built from **the very `processor` / `hasBanking` pair each roster row already carries**, so no second resolution exists to drift from the chips or the Missing-bank-info list — and **never from the feed**, whose `processor` is `preferred_processor` (the RECEIVE election) on a capped 80-row slice. **HiGlobe stays a WALLET rail** (email + account name; only 29 of 216 even have a bank name) while **Wise stays a BANK rail**; `isWalletRail` derives from `payoutRequirementFor`, which must stay in the same switch shape as `isPayoutComplete`. An **unrouted person is never payable**. **Which rails get a row is a RULE, not the enum** (`railRows`): a rail with payees survives retirement (Wise 386, Jeeves 3), still-offered rails show muted at zero, and a rail BOTH retired and empty is dropped — Wepay today, **by rule so the row returns by itself** if anyone lands on it. The **department filter scopes the BAND too** via `railMixByDept` (dept folds sum back to the org fold, by test), both labels print the dept when scoped, options come from the ROSTER so a quiet dept stays reachable, and unresolvable payees get a **"No department"** bucket — a filter never hides a row. Width is `max-w-[1600px]`, the app's wide-content container. **Processor brand logos are now asset-checked** (`processor-logo-assets.test.ts`): `logoSrc` is hardcoded in THREE registries that must agree with each other AND resolve to a real file in `public/` — a broken path silently renders a plausible gradient monogram and never errors, which is how `/kolan.png` sat referenced-but-absent on the highest-volume rail for a day. **Kolan ships TWO assets, pinned per SURFACE** (2026-08-28, supersedes mark-only): the plated dispatch card takes the dark lockup `/Kolan.png` (case-exact — Linux static serving is case-sensitive, `fs.existsSync` is not), the bare un-plated employee/contractor chips keep the eclipse mark `/kolan.svg`, because a 4.4:1 lockup in a 16px square is a ~4px sliver that vanishes in dark mode. The old blanket ban existed because the OFFICIAL lockup's wordmark is WHITE and the plate is `bg-white` in both themes — that hazard is now a **measurement**, not a prohibition: the test decodes the PNG with node `zlib` and rejects ink under 90% dark (shipped asset 96.5%, inverted copy 3.5%). Cross-registry equality still holds for every rail that has one asset. **Kolan and Higlobe are EMAIL-ONLY rails: the email IS the payout destination** (656 of 730 Kolan and 177 of 221 Higlobe payees have NO `account_number`), so the pending Export CSV must carry `hurupay_email`/`higlobe_email`/`higlobe_account_name` or their rows export unpayable — 833 of them did until 2026-08-26. The rule is now structural, not a column list: `dispatch-client-csv.test.ts` asserts every `PROCESSORS[].detailFields` key is a `PENDING_COLUMNS` key, so a new rail cannot ship a blind column. Payment Dispatch §4.2.3(c). |
| Cycle close-out | `cycle-closeout.md` | [[cycle-closeout-record]] · [[payment-cycle-complete-celebration]] · [[cycle-reopen-archive-and-suppression]] · [[pay-cycle-reports-tab]] | The close-out is the **only per-cycle record** (both report tabs retired 2026-08-12) and is **allowed to record failure** — never add a completeness gate to it. Its human-readable form is the **Stop-dialog download**: closing → FINAL CSV rendered verbatim from the POSTed record; just stopping → PREMATURE XLSX stamped NOT YET CLOSED (a discriminated union — a snapshot cannot wear a FINAL header). Bank details last-4 only. Closing is **once, plain INSERT** (first close wins), and the close-out POST runs **before** `setLocked(false)`. Paid totals are server-computed via `tallyPaidDispatches`; the payable-unpaid list is client-reported by design, and the **Excluded tab is never counted as unpaid**. **Closing fires the `payment_cycle_complete` email + in-app confetti, unpaid people or not** (Kane 2026-08-14, superseding both the original "closing never fires it" and that morning's "only if it owes nobody"): the honesty lives in the PAYLOAD, never in withholding — `trigger: 'cycle_closed'` + an honest `stats.unpaid_count`, validated by its own arm of `isReportableCycleComplete`. **The strip's `fully_paid` arm never weakened** (still `paid === total`); two arms exist precisely so neither has to. Both arms refuse a report naming nobody paid. One body builder serves both. It runs **after** the stop, fire-and-forget — never able to abort or reorder it — and the server's per-cycle `app_settings` claim means two triggers still send exactly one email. **A closed cycle can be REOPENED** (2026-08-14 — "there is no reopen" is dead, but only in this form): `reopenCycle` copies the record VERBATIM to `dispatch.cycle_reopened.<file>.<iso>` **before** freeing the live key, so a declaration is archived and never destroyed; the archive prefix must stay **outside** `dispatch.cycle_closeout.` or every reopened week reads as still closed. **A celebration marker is NOT proof the week finished** (2026-08-18): it proves a 2xx delivery for whatever the client claimed. The Aug 9–15 week mailed all 10 accounting holders "100% PAID · 1 PAYMENT SENT" with **1026 people staged payable** — the `fully_paid` arm fired on a queue the wizard's unlock/re-lock had blanked (root cause client-side, `payment-dispatch.md` §12.7.1). The gate was right and the input was a lie, so **never relax `isCycleFullyPaid` to "fix" this**; the burned once-ever claim is freed only by `scripts/clear-cycle-complete-suppression.mts --apply --force-sent`. Write order is **burn the claim → archive → delete**, and burning `dispatch.cycle_complete_notified.<file>` is the *entire* mechanism for "a reopened week never celebrates again" — never clear that key, never add a parallel gate. `DELETE` needs `CYCLE_REOPEN_ROLES` (payroll_manager/admin) on top of the edit gate. |
| Payroll Wizard | `payroll-wizard-final-pay.md` · `payroll-wizard-configuration-tab.md` · `payroll-wizard-notes.md` · `payroll-readiness.md` · `payroll-wizard-week-replay.md` · `payroll-wizard-step-load.md` | [[payroll-wizard-master-quoted-name]] · [[payroll-wizard-tab-persist]] · [[wizard-setup-readiness-checklist]] · [[wizard-every-dept-visible]] · [[wizard-rate-snapshots-toggle]] · [[per-cycle-fx-zero-placeholder]] · [[wizard-step2-header-cards]] · [[payroll-readiness-tab]] · [[payroll-notes-adjustment-column]] · [[notes-adjustment-bridge-loss]] · [[payroll-notes-fab-readiness-ring]] · [[payroll-notes-offboarded-tab-shipped]] · [[payroll-wizard-configuration-tab]] · [[tech-bonus-week-selector]] · [[wizard-week-replay-fidelity]] · [[payroll-exports-itemized]] · [[payroll-notes-panes-cache-live-freshness]] · [[readiness-activity-feed]] · [[fx-no-cross-check-npd-divergence]] · [[lawang-rate-shadow-duplicate-identity]] · [[payroll-wizard-manual-validation]] · [[payroll-wizard-step-load-progress]] · [[hsl-monthly-bonus-cards-only]] | **HSL and Additions are ONE step (4), and HSL is its OWN SECTION TAB inside it — not an entry on the Departments rail** (Kane 2026-08-28): the step carries a `Departments | HSL` strip above the workspace, and picking HSL replaces the workspace entirely (`renderHslWorkspace()`, moved verbatim) because it changes WHICH TABLE THERE IS, not which department the table shows. The section is separate state from `activeDeptTab` (the rail can no longer select HSL at all, so HSL rows can never reach the shared table, and the paused-dept snap cannot bounce the operator out of a section) and the ACTIVE section is derived, never stored, so a stored `hsl` cannot outlive the tab that offered it. HSL prices Mon–Sun weeks with a weekend premium, so its rows cannot share the other departments’ columns; pausing HSL in Configuration removes the tab like any other excluded dept. Everything after it shifted down one, then the PAB review step landed at 6 the same day, so the rail is **5** Contractors · **6** PAB · **7** Validation · **8** Dispatch · **9** Reports — the progress bar is `currentStep / steps.length`, so an id gap would read past 100% and mark Reports complete at Dispatch. The merged step’s load line waits on the PAB merge **AND** the HSL amounts, and the HSL fetch stays gated on STEP entry, never on the active section (section-gating lets green claim figures nothing ever fetched). **The HSL section opens on the rail and the table (2026-08-28)** — the header banner ("HSL Payroll — Initial Pay + KPI Bonuses") and every **weekly** KPI Bonus Period card were removed on Kane's ask: the banner restated the count / initial pay / KPI total that the table's sticky `tfoot` already carries, and a weekly card restated a bonus that pays itself through the KPI Bonus column and dispatch. The **monthly** cards (SSD Medical Records, Collections, Healthcare Team Lead) STAY — those bonuses are not auto-dispatched and the card is the only place in the wizard that states what Accounting must key into Adjustment. Monthly is `period_type === 'monthly' || dept cadence === 'monthly'`, an OR that fails toward KEEPING the card. Display names come from the master **quoted nickname**, set at exactly one point (`calcResults` `name:`). A per-cycle FX of 0 hard-gates the Dispatch step (8) and makes publish a no-op — and that zero gate is the **only** thing guaranteed about the rate: it is free-typed, cross-checked against nothing (no band, no prior cycle, no sheet cell, no WoW alarm), and a missing/unparseable key falls back to `OFFICIAL_USD_TO_PHP_RATE` = **1**, making every USD figure ≈61× too large. A HRIS-vs-NPD paystub gap is an **FX divisor** question before it is a math question. Readiness' **Set rate** fixer keys the write to the SELECTED ROW's email, which for a duplicate-identity person is not always the email the hours ride — a correct rate can persist three times and still lose to a stale individual rate on the hours-carrying alias (`chrisl@`/`lawangc@`, 2026-08-18). **A replay shows the week as it was paid, or says it can't**: saved-per-`sourceFile` values are read verbatim and live data may never leak in (`business-logic.md`), but an **absent or empty** saved value falls back to live computation and never to ₱0 — that fallback outranks the freeze. On step 2 **amber means warning, never a control** — the missing-rate icon and the "Not set for this cycle" pill own it, which is why USD→COP is teal. The step rail's load line is a **claim that a tab's figures can be judged**: prediction ramps to 0.9 and eases to 0.99, and **only the data landing reaches 100%** — never let a bar fill on prediction, and never wire `orphanageDetailLoadedFor` into the mapping (its loader doesn't settle on failure, so the line would run forever on exactly the case it must survive). |
| Payroll Wizard PAB step & Forgive month | `payroll-wizard-pab-step.md` · `pab-exclusions.md` · `orphanage-pab-coverage.md` | [[pab-wizard-step-forgive]] · [[pab-calendar-parity]] · [[pab-calendars-sun-sat-sweep]] · [[pab-payout-week-gate-and-pill]] · [[orphanage-pab-auto-coverage]] · [[employee-pab-dispute-removed]] | **Forgiveness writes approved `pab_day_disputes` rows — the month-level GRANT BLOB was scoped and REJECTED.** "Whole month, not per-day" describes the BUTTON; the batch writes one dispute per failed day, because that row is the only PAB input every verdict already reads (dispatch, member-monthly-pay, both wizard breakdowns, EmployeeDashboard, MyHours, EmployeePabCalendar, Overview). The mirror store proves the alternative fails: `grep -rn "exclusion" src/components/employee/` returns **zero hits**, so an accountant-excluded person is still told they are eligible on their own dashboard — and an invisible GRANT is worse, being a benefit the person was told they received. **Both forgive paths write `override_hours: 7`**: server-identical to the old `null`/`5` (`applyPabAdjustments` bumps ≥4h→7h either way, so no pay moved), but `EmployeeDashboard` applies the override as a plain SET and SKIPS `null` — before this a forgiven person read *"No longer Eligible for PAB — violated on <the days just forgiven>"* while being paid. 7 makes the dashboard agree with **no dashboard edit**; the cost is a `7:00` cell + Forgiven chip instead of real hours (the opposite of orphanage coverage, which is additive and needs no SET). **Severity EXPLAINS a verdict, never reaches one** — membership comes from `effectivePabStatus`, `in_progress` people are excluded, and `pab-ineligibility.test.ts` pins `severity === 0` ⟺ `computePabEligibleEmails`: **if it fails, PAB money has moved.** For HSL the count already drops weekends, overnight-qualifying days and reconciled weeks, so someone short four days can read 0 — which is exactly what makes "1 or 2" a genuine near-miss. **The evaluated window is WIDER than the PAB period**: HSL anchors back to the Sunday on/before the start and a missing day scores as ZERO, so under-supplying days MANUFACTURES failures — use `hslCoverageStart()`, and never pad zeros to fix a boundary failure. OPEN: `allDaysColumnGroups` filters from `pabMonthRange.start` and is not wide enough, so an HSL person in a mid-week-opening period can under-count the first week vs the engine; widening it would move `perfectAttendanceEligible` (money-adjacent), so the step drops severity-0 rows instead. The batch is **all-or-nothing, re-reads before reporting** (never "forgave N days"), idempotent, refuses denied rows, and writes ONE `pab_dispute.month_forgiven` audit row atop the per-day `pab_dispute.approved` ones. No migration; no new notification type (rides `dispute.approved`, already in the CHECK — a new type dies silently, as `pab.excluded` did for 17 days). **The rail is 9 steps and ids must stay contiguous** (1 Initialize · 2 Initial Calculation · 3 Orphanage · 4 Additions+HSL · 5 Contractors · **6 PAB** · 7 Validation · 8 Dispatch · 9 Reports) — the progress bar is `currentStep / steps.length`. Numbers live in EIGHT places incl. `AdminWebhooks.tsx`, `validation-breakdown.ts` and `payroll-wizard/audit/route.ts`; the two REAL gates must be re-asserted by number after any move (red-flag confirm **7**, FX-zero **8**). Closing check: `grep -rn "Step [1-9]" src app docs`. |
| Payroll Wizard tutorial mode & processing narrative | `payroll-wizard-tutorial-mode.md` | [[payroll-wizard-tutorial-mode]] | **The guide is a CHAT HEAD, never a panel or modal** (Kane 2026-08-17 rework) — a bottom-right bubble whose tap toggles a compact speech balloon; the highlighting is the spotlight rings on the page. **And it never gates**: statuses are advisory badges, the spotlight layer is `pointer-events-none`, every step is skippable — the wizard's real gates (FX-zero on Dispatch, red-flag confirm on Validation — steps 8 and 7 since the PAB step landed) stay where they are; it mounts for the lock DRIVER only (spectators are in follow mode). **Targets are resolved per render** (`resolveStepTargets`), not read off the step def: step 2 rings only the FX legs still UNSET plus that box's own CTA, step 4 has **two faces** since the 2026-08-28 merge and rings only what is mounted (`additionsHslTabActive`): on its HSL section it rings the HSL table and takes turns across its *rendered* money columns (PAB/Tech are conditional), on its Departments section it follows the operator into the System Bonus modal and gives an already-set PAB month no ring at all. **FX staleness is displayed, never adopted** — the previous cycle's `payroll.wizard.fx.<file>` record is read for advisory copy only and must never prefill this cycle's input (that would silently un-confirm the week); globals can't prove staleness since `OFFICIAL_USD_TO_COP_RATE` is 4000. The Processing Narrative's window is the **calendar Sun–Sat week**, not the cycle and not the lock lifetime — stopping processing does NOT stop the trail (it runs to the next Sunday), and it is render-only over `audit_log`: no new tables, no new audit actions, the cycle close-out stays the ONLY per-cycle record. The whole feature is **one-shot removable**: two `tutorial/` folders + the `audit-week` route + the `[WIZARD-TUTORIAL]`-marked blocks and `data-tutorial-target` attrs in `PayrollWizard.tsx` (see the doc's Removal section). |
| Payroll Wizard Manual Validation (MV) | `payroll-wizard-manual-validation.md` | [[payroll-wizard-manual-validation]] | **MV is NOT a `payment_dispatches` column, and that is measured**: all 6,932 rows are terminal (`paid` 6,880 / `threshold` 44 / `problem` 8) with **zero** lacking a `sent_date`, because a row exists only after money moved — so an MV ticked on the Validation step has nothing to write to (and `note` is already used on 2,523 rows). It lives in `app_settings` at **`payroll.wizard.mv.<sourceFile>`** as `{lowercased WORK email: {by, at, note}}`, mirroring `payroll.wizard.exclusions.<sourceFile>`. **No migration.** **Absence IS the unticked state** — un-tick DELETEs the key; never add a `validated:false` record. **The read and write parse paths are asymmetric on purpose**: read is tolerant (drops + counts bad entries so one corrupt record cannot blank a cycle), write **REFUSES** an unparseable blob (409) and merges into the RAW object so it preserves keys it does not understand — a tolerant write would round-trip an unreadable blob through `{}` and destroy everyone else’s validations. A failed **read** likewise never blanks state (route 500s; wizard and hook keep what is on screen), because re-ticking overwrites the original validator’s name. **Writes never use the generic `/api/app-settings` POST**: it is last-write-wins, so two clerks each reading `{}` and saving leave only the second entry with **no error** — still true of `payroll.wizard.exclusions` today. MV uses `casUpdateAppSetting` (read `{value, updatedAt}` → merge one key → write only if `updated_at` has not moved), conflict retried 5× then 409; **never “fix” a 409 by dropping the CAS predicate.** **`by`/`at` are stamped in the route, never taken from the body** (else you can vouch in another name or backdate), and every write audits `accounting.payroll_wizard.manual_validation.set`/`.cleared` — the generic route audits only admin-only/sensitive/dispatch-lock keys. **Mark Paid keys on `QueueRow.id` (the WORK email), NEVER `.email`** (the payout address, shared and recycled in the master list — an alias match could show a validation belonging to a DIFFERENT person); **no fallback**, show nothing instead. Both Mark Paid surfaces read one `useManualValidations` hook because they already disagree about what they POST (`/payroll-clerk` omits `amount_cop` + `system_bonus_*`). The banner renders **only when a validation exists** — no “not validated” state, or every payment wears a warning and the clerk ignores the band; emerald, never amber. Display-only: `MarkPaidPayload` (11 scalars) is untouched. **No “validate all” master tickbox** — that claim is what the column exists to make impossible. **The Adj column already existed and is live** (290 non-zero in the 2026-08-09 cycle, 1,009 across all snapshots, fed from the Notes board); the real gap was the **expanded row**, which listed it only when non-zero, so it now lists **unconditionally** — still READ-ONLY, the Notes board sets it. **Full screen is a PORTAL and cannot be a route**: `PayrollBreakdown[]` exists only in `PayrollWizard.tsx`’s React memory, so a page could only re-derive from `/api/payroll-current-pay` (a second pay implementation free to disagree with the wizard on the screen that certifies it) or read `final_pay.<file>`, withheld pre-lock. The overlay gets the SAME `filteredRows` array and the SAME table component, so it mirrors by construction. **Payment Dispatch's View Paystub shows the vouch too (2026-08-27)** — same emerald band, same display-only prop, in a right-hand accounting rail above the dispatch log; `PayrollDispatch` carries `workEmail` (`row.id`) on its `viewPaystub` state SEPARATELY from `email` (the payout address the stub is fetched by), because collapsing them is the alias bug. The rail is out of flow (`xl:absolute xl:left-full`) so the statement stays centred, and renders nothing when empty so employee mounts are unchanged. **`PaidRecordsPanel`'s View is excluded**: a `payment_dispatches` record carries only `recipient_email` (the payout address) and no fallback is permitted. Column arithmetic moved `cols` 14→15 / 17→18, the last group span 2→3 and the footer to `cols-3`; drift there visibly misaligns the expanded row and footer. |
| Paystubs | `paystub-dispatch.md` | [[paystub-staged-snapshot-stale]] · [[prorated-paystub-design-approved]] · [[midweek-transfer-proration-ruling]] · [[paystub-weekend-hours-hsl]] · [[employee-paystub-modal-and-paid-notification]] · [[paystub-export-week-dedupe-and-startdate]] · [[paystub-email-rendered-in-app]] · [[never-paid-and-misdelivered-paystubs]] · [[payroll-available-notification]] · [[payment-cycle-complete-celebration]] · [[paystub-tab-exact-recovery]] · [[midweek-transfer-paystub-label]] · [[paystub-export-current-department]] | `paystub-fresh.ts` merges the snapshot **over** staged data; marking paid freezes it. The weekend block is a display **carve-out** — totals do not change. The mid-week transfer label ("Lead Gen to HSL") is **staged into the payload**, read from `department_transfer_requests` (**`applied` rows only**) and never from the `proration` block — only a *rate* change prorates, so a same-rate transfer has no proration block and is exactly what the label exists to disclose. A **paid** stub never gains it. The **export header** answers a different question and answers it differently: it names the employee's **CURRENT** department, marked `(current)`, on every week it covers — paid or not — because a department is a fact about the person, not the week; only the money and the per-week rows are frozen. One resolution for all three export callers: `currentDepartment` off the routes' master-record read. |
| HSL sub-departments | `hsl-subdepartments.md` | [[hsl-subdept-restructure]] · [[hsl-placement-only-subteams]] · [[hsl-bulk-subdept-assignment]] · [[hsl-parent-department-cutover]] · [[hsl-collections-tl-simple-texting-removed]] · [[rate-catalog-source-of-truth]] · [[payment-catalog-department-tab]] · [[hsl-gml-roster-merged]] · [[pay-structure-department-members]] · [[dept-label-display-sweep]] | **HSL is ONE department.** Every picker/filter shows a single `HSL` (`collapseHslFamilyLabel`, applied at `/api/departments` — **never** in a sheet-sync path); the sub-team is a separate REQUIRED selection composed into the same cell as `hsl:<key>`. A bare `HSL` is not a placement (`isPlaceableDeptLabel`). Rates resolve **sub first, parent as permanent fallback** — every "what they're paid" surface must mirror that. Dept-scoped **bonuses** stay keyed on the PARENT `hogan_smith_law`. Test HSL membership with `isHslFamilyLabel`, never `dept === 'hsl'`. **"Placeable" and "has a KPI calculator" are TWO keyspaces** (§1.1): `HSL_DEPT_KEYS` = owns a calculator; `HSL_PLACEMENT_ONLY_SUB_KEYS` (`hsl-subdept.ts`) = placeable + priceable but scored under another team's calculator (Simple Texting → Callback Team; Hearing Prep Team – Mail Sorting → Pre-/Post-Hearing Prep, added 2026-08-14). A real team with NO bonus program takes §7a with **`noKpi: true` + `rules: []`** (Executive Guest Services, then Executive Assistants — both 2026-08-14): roster-only calculator card, and Payroll Readiness reads it `no_bonus` ("Ready by definition") instead of a permanent weekly `draft` — a test pins noKpi ⟺ zero rules. **Placement and KPI roster are TWO DIFFERENT WRITES** (§10): the master cell prices them, but `hsl_team_members.dept_key` is the ONLY thing the Payroll Wizard's HSL rail reads (`PayrollWizard.tsx:14504`, gated by `HSL_DEPT_KEYS`) — place someone without seeding the roster and they are correctly priced yet still bucket "Unassigned", dropping out of the manager KPI cards. That gate is also why a manager-visible team can never be §7b. **Half of that trap closed 2026-08-19**: `/api/hsl-bonus/team-members` now MERGES the GML into the roster (`mergeHslRoster`), so a placement alone reaches the rail — but `hsl_team_members.dept_key` is still the only thing the rail reads, so a cell resolving no branch (bare `HSL`, a placement-only key) still buckets Unassigned. **Only the canonical `hsl:<key>` form is admitted, by ruling**: the branch's plain-display-name fallback in `normalizeDeptToKey` was DROPPED before merging (Kane 2026-08-19) because that function IS the family key — inferring membership from a bare label would have put `cjm@`/`jamec@`/`ellyt@` (master cell `Executive Assistants`) on HSL pay and on the `executive_assistants` roster. Two tests name them; never re-add it. **579/579 placed is a RUN RECORD, not a current invariant** — but the *"39 active rows are back on plain `HSL`"* reading of 2026-08-19 was WRONG and is corrected here (re-measured 2026-08-21 with a negative control): `global_master_list` holds **108** bare-`HSL` rows — 69 off-boarded and 39 not — and **zero of them are in `active_employees`**. On the live roster all **565** HSL-family people are placed on one of the 16 sub-teams. The 39 are 13 people transferred out (their bare row is historical), 3 stale duplicates of people already placed, 21 off the roster with no off-board stamp, and **2 working right now while invisible to every roster surface** (`lawangc@`, `shainan@` — both in the current Hubstaff file, `off_boarded_at` null, absent from the view). So the degradation below is a LATENT shape, not a live mispricing: bare `HSL` resolves **no department base** (the parent rate row was deleted) and `resolveDeptLabelForRate` actively suppresses the rates-row leg for it, while the week model + weekend premium survive as family-level. **The parent-department cutover (§11) is COMPLETE, 2026-08-14**: 579/579 placed, all 16 sub-teams priced, 465 employee-scope rows re-keyed, and the parent `hogan_smith_law` base row **deleted** — end-state sweep: 472 individual / 107 sheet / 0 department base / **0 no-rate**. The HARD HOLD release is **narrower than §8 described**: `resolveDeptLabelForRate` prefers the master label ONLY when it is HSL-family, because a blanket master-first flip was measured to move `carla@` from a ₱175 base to none — do not "simplify" it back. **The parent's death killed the placement + base rate, never the FAMILY KEY** — `normalizeDeptToKey`'s `hsl:* → hogan_smith_law` collapse still carries the HSL week model, the +₱15/h weekend premium and dept-scoped bonus matching for all 579; an unknown/retired `hsl:*` cell now resolves NO department base (degrades, still HSL-family for pay). Adding a placement-only team to `HSL_DEPT_KEYS` mints a duplicate calculator card **and** a permanent `draft` row in Readiness → KPI Submissions that pins the score under 100 every week (`payroll-readiness.ts:571` has no grant/roster filter and no zero-roster downgrade) — the test suite fails on purpose if you try. **Retiring** a sub-team has its own path (§7c) and a strict order: **code first, rate row second** — drop the row while the key is still live and you get an offered, placeable team with no base rate. `lead_nurture` was retired 2026-08-13 (duplicate of Simple Texting); a test pins retired keys absent from both keyspaces yet still HSL-family for pay. **Bulk assignment (§9, shipped 2026-08-14, 482 people) is a RELABEL, not a transfer** — it writes zero `department_transfer_requests` rows (one dated today re-scopes everyone's +₱15/h weekend premium) so it shows in no Transfers tab; its role→key table is **declared, never inferred**, and three roles are unmapped by Kane's ruling. Two guards carry it: **Guard 6** aborts if any target `hsl:<key>` rate row differs from the parent base (that reprices whoever rides the department base — a rate CHANGE, not a placement), and **Guard 8** makes the DB write set the **Sheet-matched** set, because a DB row the Sheet disagrees with is not a placement but a pending clobber. **`hsl:<key>` NEVER reaches a human** (§12, 2026-08-24): `formatDeptLabel` wraps every department value on its way to a screen, a toast, a notification, a paystub or its email — it is a no-op on non-HSL labels, so wrapping is unconditional. The **raw** string stays in exports, `title` tooltips, search haystacks, filter/query VALUES and sheet-sync paths. `humanizeDeptKey` and `hr/DeptFilter` are the two chokepoints. A source-scan test (`dept-label-render.test.ts`) fails on any new JSX child that renders a department value unwrapped. Payment Dispatch was never affected — it resolves to "Hogan Smith Law" via `deptKeyOf`. OPEN: headcount-by-department cards still GROUP on the raw cell (labels fixed, roll-up not). |
| HSL rates & KPI | `hsl-weekend-ot-pay.md` · `hsl-kpi-calculator-2026-07.md` · `hsl-subdepartments.md` · `hsl-catalog-migration.md` | [[hsl-sheet-form-pay-rule]] · [[midweek-transfer-proration-ruling]] · [[hsl-rate-history-stale-underpay]] · [[hsl-weekend-premium-in-ot-column]] · [[weekend-rate-off-headline-disclosure]] · [[weekend-ot-merge-assessment]] · [[hsl-subdept-restructure]] · [[hsl-collections-tl-simple-texting-removed]] · [[hsl-kpi-calculator-2026-07-changes]] · [[kpi-calculator-autosave]] · [[hsl-bonus-weeks-never-submitted]] · [[kpi-calculator-week-unresolved-hang]] · [[hsl-catalog-migration-findings]] | **An unresolvable payroll week is TERMINAL, not pending** (2026-08-24): both calculators' first-load gates waited on data that a failed week-resolution never produces, so the loading skeleton became the screen — while the rose "Couldn't confirm the payroll week" alert that explains it renders INSIDE the chrome the gate was withholding. One rule for both now, `kpiCalculatorRevealed` (tested); HSL's `booted` is DERIVED every render, never latched from an effect, because the original bug was `weekError` missing from a dep list. It loosens nothing — `loadDept`'s early return, every read/write site and `kpiAutosaveGate` all still hold on `weekResolved`. Root cause was upstream: the week comes from the Hubstaff batch FILENAME, now validated at ingest (`csv-imports.md` §4). The KPI Calculator **autosaves** — see the Rates & Payment Catalog row. SSD Medical Records' sub-team accuracy/records/RFC inputs are STILL not persisted (Kane, 2026-08-17), so `recomputeSsdEntries` refuses to overwrite a non-zero saved share when its sub-team has no inputs on screen — without that, autosave would write **₱0 to a whole team** after any reload. A typed `0` is an input; only an untouched field is blank. **HSL pay = the Hogan sheet's column AN, verbatim (2026-08-11)**: ALL M-F hours × regular (never re-rated) + ALL weekend hours × `regular + 15` (**past-cap included** — the 2026-08-07 within-cap scoping is REVERSED) + `max(0, total − 40) × regular × 0.5` as a **derived** differential — the stored OT rate never moves HSL money (which retires the reg+15-in-the-OT-column bug class of 2026-08-04; the store SHOULD still hold `regular × 1.5` and the amber `ot_ratio` flag audits it). Legs price 2dp HOURS × rate, like the sheet. **Not every HSL row takes this rule**: the three legs need DAILY columns (`hslSecs && regularRate != null`), so a row with no per-day split keeps the LEGACY two-bucket formula and stages `hoganSheet: null` (9 rows in the 08-09 cycle). Triage a sheet-vs-HRIS gap in that order — `hoganSheet: null` ⇒ different formula, not different math; else a weekend rate that isn't `reg + 15` is a **dated rate change**, not a math error (check `proration.effective_date` first); else compare the USD divisor, not the pesos. `employee_rate_history` outranks `employee_hourly_rates`. **The double-pay guard is RED on `main` and has been normalised as "pre-existing"** (reproduced 2026-08-31): `kpi-calculator-depts.test.ts` fails with *"executive_assistants is in WIZARD_PAYABLE_KPI_DEPT_KEYS — it would be paid twice (catalog + HSL entries)"*. `WIZARD_PAYABLE_KPI_DEPT_KEYS` = `DEPT_INPUT_CONFIG` ∪ `KPI_CALCULATOR_RETIRED_DEPT_KEYS`, so **any dept retired from the KPI calculator and later adopted into the HSL family re-enters the payable set silently** — two independent edits in two files with nothing coupling them. A failing tripwire catches nothing; fix it rather than quoting the test count around it. **`calcBonus` does not score `ssd_medical_records` or `hsl_managers`** — it skips `team_split`/`team_pool` entirely and `hsl_managers` has empty `rules[]`, so their `calculated_bonus` comes from `recomputeSsdEntries`/`calcManagerBonus`; never assume one scoring entry point. **`monthlyMax` is gated `!== undefined`** — `monthlyMax: null` makes `Math.min(total, null)` = **0** and zeroes the department; `null` and absent are NOT interchangeable. **`medical_records` Patient Portal Log Ins is ₱100 in code and ₱250 in the doc** — an UNRESOLVED contradiction on a live weekly money path, flagged inline in `hsl-kpi-calculator-2026-07.md` rather than silently corrected. |
| Rates & Payment Catalog | `bonus-catalog.md` · `payment-catalog-departments.md` · `bonus-calculator.md` · `pay-structure-no-department.md` · `salaried-pay-basis.md` | [[rate-catalog-source-of-truth]] · [[payment-catalog-overview-redesign]] · [[payment-catalog-search-tab]] · [[payment-catalog-department-tab]] · [[system-bonus-dept-resolver-split]] · [[system-bonus-paid-tab-snapshot]] · [[custom-system-bonuses-currency-variants]] · [[tech-bonus-week-selector]] · [[no-pay-rate-dept-fallback-order-bug]] · [[offboarded-bonus-scoring]] · [[kpi-bonus-shared-personal-email]] · [[bonus-overrides-email-key-casing]] · [[ssd-medical-records-rfc-pool]] · [[medical-records-rfc-manual]] · [[kpi-calculator-retired-depts]] · [[kpi-calculator-autosave]] · [[payment-catalog-hides-offboarded]] · [[catalog-visible-dispatch-absent-means-no-hours]] · [[pay-structure-department-members]] · [[pay-structure-natural-key-upsert]] · [[pay-structure-no-department-audit]] · [[salaried-pay-basis-is-person-dated]] | The Payment Catalog is the **rate source of truth**; any comp display must mirror engine precedence exactly, alias emails included. **Off-boarded people are filtered off every people surface** (2026-08-21, `bonus-catalog.md` §3.2) — and `active_employees` cannot tell you who they are: **zero** of 1,287 active rows carry an `off_boarded_at` while **294** of those people have left (HR keeps a leaver on the sheet through final pay; the stamp lands on a duplicate row). Evidence is unioned ONCE in `offboard-evidence.ts` (Readiness' `loadOffboardDatesByEmail` now rides it too), and the predicate is **four guards that all resolve toward KEEPING the person**, because on this surface a lingering leaver is cosmetic while a vanished active worker has no settable rate: canonical-departure reason (an **allowlist** — `duplicate_cleanup` is 94 rows of migration marker, `temporary_pause` is a suspension, and the column is free text with both casings), off-date after the person's own Start Date, off-date before the week being paid, and **no hours in the current timesheet** (18 people cleared the first three and were still logging hours — `sherwins@`, `kevinc@`, `jeff@`, `jan@`). Evidence matches on **work emails only**: a shared personal inbox imports someone else's departure (`carlath@`'s resignation onto the live `carla@`), and `offboarding_queue.employee_email` is the personal address on all 460 completed rows. **Hours are only ever a reason to KEEP** — a zero-hours active member still shows. Fails open everywhere; existing rate rows and name resolution keep the FULL roster. 178 hidden / 1,109 shown; `scripts/verify-catalog-offboarded.mts` proves it against prod. **Pay Structure is a TREE and the parent claims nobody** (2026-08-21, `bonus-catalog.md` §5.5): `buildDeptRail` nests the 16 `hsl:*` sub-teams under a retractable Hogan Smith Law, and `parentOfDeptKey` is a **declared** map because the HSL parent key is NOT the child prefix (`hsl:x` → `hogan_smith_law`, while custom subs are `<parent>:<sub>` where it is) — a `split(':')` orphans every HSL team. `assignRosterToRail` gives each person **exactly one** home, most-specific first: without it `normalizeDeptToKey`'s `hsl:* → hogan_smith_law` collapse makes the parent list its whole 565-person family (Kane: *Baldonebro is a case manager already*). Parent own-membership is 0; counts ROLL UP or a collapsed parent reads 0 over a 174-person team; unresolvable labels get a **"No department"** entry (60 people) because a filter never hides a row. **Where an individual structure renders is not where it is stored** — `homeKeyForStructure` follows the person's CURRENT placement, since 65 of the 124 rows filed on `hogan_smith_law` belong to sub-team people (the Search card writes the collapsed key). Resolution is **email → unique NAME → "No department"**, and the name leg is load-bearing: Baldonebro's stale `joyb@simple.biz` row is a THIRD identity absent from her aliases, so email-only left it unresolved and it kept its stored key — she showed under the parent twice after being placed on Case Managers. An unresolvable owner goes to **"No department", never the stored key**, because a department row is a claim about a real person. The name bridge is EXACT-token and unique-owner-only (namesakes are dropped, never guessed). Live end state: **the parent holds 0 members and 0 rate rows**; 808 structures in, 808 homed. DISPLAY ONLY, self-heals on transfer; no money moves because `buildCatalogRateIndex` keys employee structures `byEmail` and never reads `departmentKey`. The member list is READ-ONLY and its rate chip is `computePersonComp` + `winningRate` — **never a local `override ?? deptBase`**, which would misstate all 361 sheet-rated people; "Set rate" hands off to the existing adder so there stays ONE write path. The KPI Calculator's cards come from **two** sources — `DEPT_INPUT_CONFIG` keys **and** the slugs of unmapped `department_managers` grant labels — so adding or retiring one takes both, via `isKpiCalculatorDeptKey` at all three grant-slug sites (`DeptBonusCalculator` · `use-bonus-scoring-queue` · `ManagerApp`). Payroll Notes → KPI Submissions is a **separate** list and is deliberately not filtered by it. **Scoring AUTOSAVES (2026-08-17) — there is no Save button** on either calculator in manager mode; every refusal the button carried lives in `kpiAutosaveGate` (`src/lib/manager/kpi-autosave.ts`, tested), and the two that are easy to break by "simplifying" it are: never persist **load-seeded** state (`DeptState.seeded` — pre-ticked common bonuses + the QC first-pass would otherwise be written by merely OPENING the tab, attributed to whoever opened it) and never re-send a **just-failed** state until the manager edits again (a failure leaves the dept dirty, so a plain debounce retries forever). **Submission stays MANUAL** — Mark Ready / Lock → Submit are unchanged, because the period-status row is what tells Accounting the week is done. A pending write flushes on tab-hide / `pagehide` / unmount, since `ManagerApp` unmounts the calculator on tab switch. QC officer mode keeps its explicit Save. A pay structure's `id` is a **surrogate** — the DB's real key is `(department_key, lower(employee_email))`, so every write resolves the slot's current occupant server-side (`resolvePayStructureWriteTargetId`); a caller that guesses an id degrades the upsert to an INSERT (714 structures were affected before `d9f34ef7`). **OPEN: COP is still silently stored as PHP**, and the one-line fix must ship with the `syncRateHistory` guard or it corrupts the PHP rate history. |
| Payment Catalog → Pay Structure → "No department" | `pay-structure-no-department.md` · `bonus-catalog.md` (§5.5) · `scripts/audit-pay-structure-no-department.mts` | [[pay-structure-no-department-audit]] · [[pay-structure-department-members]] · [[payment-catalog-hides-offboarded]] · [[maria-argote-split-identity]] | **The bucket is not a department, and landing in it means NO RATE CAN REACH YOU FROM THIS SCREEN** — `BonusCatalog.tsx:2400` hides the department pay-structure editor and `:2503` hides the individual adder, because a rate saved there would file `department_key='@no_department'` where nothing resolves it. So its population is a **rate-visibility alarm, not cosmetics**. Audited 2026-08-30 (read-only, reproduces the tab exactly by importing the SHIPPED `buildRoster`/`assignRosterToRail`): 61 rows, 60 rendered, **59 humans**, and only 26 of them are the USEE cohort the tab appears to be. **32 active Site Building people are here because of a label suffix** — `normalizeDeptToKey` knows `"site building"` and nothing else, so `Site Building (US - Freelance)` / `(PH - Freelancer)` resolve to no rail entry while the real rail entry holds 7; **28 of the 33 still carry a stale bare `"Site Building"` GML row at the identical start date, which makes it a RELABEL, not a transfer.** The fix already exists one surface over — `baseDeptLabel` (`hubstaff-reconciliation.ts:55-78`) strips the trailing parenthetical after this same rename broke reconciliation on 2026-08-09 — and the catalog rail never got it. Also here: **Ralfh Macapagal ("Orphan Ministry", a RETIRED KPI key that exists in no dept map) is actively hourly-paid** — 25 weekly files, ₱15,329.92 dispatched 2026-08-27; and **Seungyong Lee appears twice** (`seungyong@` cell literally `"Manager"`, already blocklisted in `HUBSTAFF_RECON_EXCLUDED_EMAILS`, vs. the working `seungyongl@`). **Most of the bucket is not people at all**: 120 rate rows, 111 owned by nobody the roster can place — that is `homeKeyForStructure` refusing to park a ghost on a real department, working as designed. Do not re-home them; the rows are evidence. |
| Salaried pay basis — HSL Attorneys (**designed, NOT BUILT**) | `salaried-pay-basis.md` | [[salaried-pay-basis-is-person-dated]] · [[transfer-sheet-sync-false-success]] · [[sheet-readd-dept-clobber]] · [[midweek-transfer-proration-ruling]] · [[transfer-does-not-rerate]] | **Nothing is shipped** — no `employee_salary_history`, no `pay_basis` column, no `salary-week-pay.ts`. The ruling exists so it is not re-derived: **a salary is a DATED FACT ABOUT A PERSON, never a property of a department.** Three designs keyed the pay basis on the department label and all three were killed for it (6.5 / 5.7 / 5.0). The department cell cannot carry pay because it is the most-clobbered field in the system (≥ 7 applied transfers provably never landed while reporting `sheet_synced=true`) and `masterDeptByEmail` is **first-wins over unordered rows** (`current-pay.ts:951-954`) — with a duplicate GML row, PostgREST row order decides the pay basis. Worse, it **fails toward HOURLY, not toward zero**: an unpriced `hsl:*` key inherits the parent base by design (`hsl-subdept.ts:197-200`, *"a placement here can never resolve ₱0"*), so a missing salary row produces a plausible number on a plausible paystub. Also load-bearing: `fetchMasterMin` reads `active_employees` and degrades silently on a partial read, so a mid-week offboard loses the label on exactly the final-pay week; the re-lock freshness guard is OFF for salaried people **by construction** (`getCatalogRateClaimsByEmail` reads EMPLOYEE-scope PHP structures only) and re-lock overwrites PAID rows with no post-pay detector. Two hardening rules the adversarial pass earned: `resolveSalaryBasis` returns `salary|hourly|unknown` and **never `null`** (a degraded read must not `if (salary)` its way into the hourly chain), and `regularRate`/`otRate` are **DELETED** from `CalcRow` in favour of a required discriminated `basis` — deleting rather than adding a sibling is what makes all 14 read sites a compile error with no `?? 0` escape. **The master cell pays before the code does**: an `hsl:attorneys` placement with no dated salary row must REFUSE TO PRICE. |
| HSL KPI Calculator → Payment Catalog migration (**scoped, NOT BUILT**) | `hsl-catalog-migration.md` · `hsl-kpi-calculator-2026-07.md` | [[hsl-catalog-migration-findings]] · [[kpi-calculator-autosave]] · [[ssd-medical-records-rfc-pool]] · [[hsl-bonus-weeks-never-submitted]] | **`src/lib/hsl-bonus-catalog/` does not exist** — Kane approved an INERT migration 2026-08-29 and it was never written; what survives is four verified facts about the live engine (see the HSL rates & KPI row for all four). **Byte-for-byte replay of the 3,941 stored rows is provably unachievable**, and the rule that follows is the durable part: any HSL→catalog parity tool must deliver a **classified divergence report** — every row reconciled or assigned to a named class with counts and peso deltas — never a green checkmark over a quietly narrowed row set. The four classes: `ssd_medical_records` (172 rows — team inputs `pct`/`records`/`rfc` are **deliberately never persisted**, `kpi_data` holds only `{"sub_team"}`, so one stored sum over three unknowns is not invertible), retired dept/metric keys (~150 — `case_manager` rows use `five_star_reviews`/`dme_prescriptions`/`completed_tasks` against live `reviews`/`dme`/`task`, recomputing to ₱0), `callback_team` before 2026-07-21 (the rule set was REPLACED after the rows were saved), and effective-dating (`BonusDef` has **no effective-date field** — the catalog shape structurally cannot version a rule). Two wiring-time decisions remain unmade and block any non-inert follow-up: auto-apply vs. overridable KPI semantics, and whether history is backfilled. |
| Offboarding | `offboarding-automation.md` · `manager-my-team.md` (Suspend / Reactivation) | [[offboard-delete-only-routing]] · [[temporary-pause-offboard-reason]] · [[manager-suspend-button]] · [[offboard-final-pay-grace]] · [[scheduled-deletion-cron-never-ran]] · [[rehire-invisible-offboard-reuse]] · [[clearoffboarded-reactivation-collision]] · [[final-pay-roster-overlay]] · [[readiness-bank-offboard-aging]] · [[offboarding-docs-and-code-map]] · [[offboarded-tab-merged-origin-column]] | **The HR Offboarded list is ONE tab with an Origin column** (2026-08-28): "Offboarded by HRIS" and "Offboarded" were never two populations — `/api/hr/offboard` writes both `offboarding_queue` and `offboarded_sheet`, so all **488/488** completed queue rows already sat in the ledger and the HRIS tab added **zero** people. Origin is a **stored** `offboarded_sheet.origin` column (`NOT NULL DEFAULT 'hris'`, CHECK `('hris','google_sheet')`), backfilled 491 `hris` / 3,354 `google_sheet` from `off_boarded_by IS NULL` — and stored precisely because that heuristic was an accident of history that the JSON backfill breaks (an imported row is written today with no actor, which the old rule reads as a modern HRIS row). An unclassifiable row shows an amber **Unknown**, never a folded guess. The **165-row one-off JSON import** (`scripts/import-offboarded-from-json.mjs`, `--apply` gated) does NOT reopen the spreadsheet: the retired sync was dangerous for being RECURRING and REPLACING, and this is **INSERT-ONLY** — it skips anyone already on the ledger, which is what keeps hand-corrections durable. Proof it matters: the export **still** carries `franm@`'s `4/20/2027` typo (the cell the sync was retired over) while the DB holds the fixed `2026-04-20`; the script names her every run and exits non-zero if she'd ever be inserted. Dates mirror `normalizeMasterDate` + `sanitizeOffboardDay` and land NULL rather than guessed; reasons are stored VERBATIM (every consumer reads them through a departure ALLOWLIST, so an unknown label keeps the person visible); `off_boarded_by` stays NULL rather than fabricating an actor. **22 work-email collisions are SKIPPED** — a second off-board record on a recycled work email becomes off-board *evidence* against whoever holds it now (**OPEN**: those 22 are still absent from the list). Because `offboarded_sheet` is evidence source #2, the import DOES reach money surfaces: 4 rows name active people (`joyp@`/`mackp@`/`shanninp@`/`cathyp@`) and are safe by the catalog filter's existing four guards, all of which resolve toward KEEPING — including the allowlist that excludes `temporary_pause`, which is exactly `cathyp@`'s row. **Every** offboard fires `offboarding_delete` — including one landing on a person currently suspended via `temporary_pause` (escalates: re-stamp + delete). `offboarding_deactivate` is the suspend/temporary pathway **only**, and `temporary_pause` can never enter the manager offboard queue (server-rejected). The 14-day deferral is retired — never restamp `scheduled_deletion_at`. Manager **Suspend** mirrors that offboard envelope exactly; manager **Reactivation** does NOT — it has its own `employee.reactivate` / `phase reactivate` / `reactivated_by` envelope at `hris-reactivate-suspended` (verified 2026-08-10), with no `reason` and no `scheduled_deletion_at`. |
| MESA | `mesa.md` · `accounting-mesa-export.md` | [[mesa-notes-and-roster-bridge]] · [[mesa-flag-vs-ledger-rejoin-gap]] · [[mesa-accounts-per-stint]] · [[mesa-optin-requests-derived]] · [[mesa-optout-deduction-and-sheet-clobber]] · [[mesa-deduction-nonmembers-ledger-gap]] · [[mesa-optout-effective-date]] · [[mesa-disbursement-receipts]] · [[mesa-week-delete-cascade]] · [[mesa-off-gml-indicator]] | An opted-out member is **never** charged. Weekly 100 + 300 deposits are idempotent. Opt-out state must be read from the **ledger**, not the flag alone. |
| Roster, master list & identity | `identity-resolution.md` · `csv-imports.md` · `hr-global-master-list-export.md` · `sales-dept-split.md` · `department-transfers.md` | [[master-list-sync-race]] · [[sheet-readd-dept-clobber]] · [[transfer-apply-reconcile-by-target]] · [[midweek-transfer-proration-ruling]] · [[sales-dept-split]] · [[referrer-email-matching]] · [[onboarding-name-split]] · [[people-profile-name-parts-editor]] · [[roster-bulk-check-rls-gap]] · [[postgrest-1000-cap-sweep]] · [[sheet-import-tables-quoted-columns]] · [[monday-hris-board-sync]] · [[dead-tables-drop-candidates]] · [[lawang-rate-shadow-duplicate-identity]] · [[maria-argote-split-identity]] · [[midweek-transfer-paystub-label]] | **PostgREST truncates at 1000 rows even with `.range()`** — always page via `selectAllPaged`. Transfers reconcile by **target** department, not source. **A duplicate master identity splits pay from rate**: hours ride one alias, an individual catalog rate saved on the other sits at the top of a chain nobody prices from — fix the email carrying the Hubstaff hours, and merge the rows for the real cure. |
| Onboarding & documents | `onboarding-name-parts.md` · `onboarding-calltools-username.md` · `onboarding-gmail-surname.md` · `onboarding-ip-assignment.md` · `onboarding-pay-plans.md` · `new-hire-checklist.md` · `workspace-account-verify.md` · `documents-tab.md` | [[onboarding-middle-name-and-order-check]] · [[onboarding-name-split]] · [[onboarding-contracts-download-tab]] · [[coe-generated-not-uploaded]] · [[documents-tab-signing-flow]] · [[documents-tab-queue-ui]] · [[typed-cursive-signature]] · [[orientation-email-invalid-to-incident]] · [[orientation-email-lead-gen-only]] · [[gift-feature-info-only]] · [[calltools-roster-backfill]] · [[hurupay-kolan-rebrand]] | **Paperwork is a historical document**: `hr_onboarding_submissions.payout_brand` stamps which wallet brand the hire actually saw, so anything signed before 2026-08-24 keeps printing "Hurupay" in the HR detail modal and the export packet while new paperwork says "Kolan". NULL ⇒ Hurupay, and an unreadable stamp falls back to Hurupay so a bad value can never retitle a signed record. Deliberately NOT in `LIST_COLUMNS` and stripped-and-retried on write, so the un-migrated database keeps working. The COE is **generated, never uploaded** — the signature lives in the document body. Structured name columns are the source of truth for the composed master `Name` — but **`middle_name` is deliberately NOT one of the composed inputs**: `composeFullName` takes first + last + extension only, because the display trigger's go-by rule takes the last given token and would rename "Jane Marie Santos" to `Santos, Jane Marie "Marie"` everywhere the Payroll Wizard prints her. No derivation (work email, Gmail Surname, CallTools, Nickname) ever sees a middle name. The Welcome step's name-order check fires **once** and **never blocks** — acknowledged the moment it opens. The New Hire Checklist **Lock in** email is the **Lead Gen orientation invite**, so the sender withholds every non-Lead-Gen hire from `rows[]` (`isLeadGenDepartment`, the same predicate as the CallTools gate) and **fails closed** on a blank or unrecognised department — the n8n Filter node is only the second layer, and a withheld hire always surfaces via `skipped[].reason`. **A signature can be DRAWN OR TYPED as of 2026-08-28** (`documents-tab.md` § Draw or Type) and the two are indistinguishable downstream — Type mode rasterises to the same trimmed PNG data URL into the same `document_signatures.image_data_url`, so the feature carries **no migration and no server change**. Three guards exist because each failure it prevents is **silent**: the six cursive faces are **self-hosted** (`public/fonts/signature/`, SIL OFL) and each is gated on `document.fonts.load()` **and** `check()` — canvas 2D does not report a missing font, it quietly substitutes the generic family, so a CDN miss would save a signature that simply is not cursive with no error anywhere, and a face that fails to load is **disabled, never substituted** (do NOT move these to the Google CDN the way Inter is loaded at `src/index.css:1`); coverage is **per face** because Homemade Apple publishes no `latin-ext` and alone cannot draw "Łukasz", so `coversText` refuses rather than printing a `.notdef` box on a document going to a bank; and the raster is sized against the PDF rather than the screen, since both renderers scale-to-fit and **never upscale** (`coe-document.ts:452` maxH 46, `sign-pdf.ts:234` maxH 58) — ink is fixed at 160 px and the width cap sits at 2400, deliberately **above** the 8:1–12:1 band real cursive names occupy, because capping costs ink height and 1600 would have shrunk long names. The **pointer-to-ink offset** in the draw pad was the dialog, not the pad: sizing the bitmap from `getBoundingClientRect()` on mount measured the box mid `zoom-in-[0.94]` (`components/ui/dialog.tsx:58`), so the surface spanned `0.94·W` stretched across `W` and ink landed ~6% of the pad width off at the right edge (~1 cm), invisible to `ResizeObserver` because **a CSS transform does not change the layout box it observes**; the fix is `offsetWidth/offsetHeight` **plus** dividing out the live rect ratio per axis at draw time (`signature-canvas.ts`, DOM-free and tested), and deferring the measurement past the animation is **not** a fix because it leaves page zoom broken. `app/onboarding/[token]/page.tsx` keeps its own private pad at 4 sites and **still carries this bug** (out of scope, Kane 2026-08-28). Typed signatures are allowed on **every** type incl. COE — one non-blocking advisory on the employee's request form, no per-type branch and nothing gated. Also found: the COE one-page tests embedded a **1×1** PNG that renders **1 pt** tall, granting the layout 45 pt of slack production never has on the very element that decides the page count — they now also run at the real 184 px raster, and **a green one-page test driven by a 1×1 placeholder is not evidence**. |
| Orientation attendance (Manager -> My Team -> Orientation) | `manager-orientation-attendance.md` · `manager-my-team.md` · `new-hire-checklist.md` | [[manager-orientation-weekly-attendance]] | **A hire attended iff `orientation_attended_at` is set — `status` is a sub-label, never the test.** Two live rows break every status-based rule: id 717 carries BOTH stamps with `status='no_show'` (it is an ATTENDANCE), and id 1034 carries `no_show_at` with `status='ready'` and no attended stamp (a reverted no-show — it is AWAITING). `attendanceRate` is `attended / total`, so an unmarked hire counts AGAINST the week — that is what "was not marked attended" means. **The week is `hr_new_hire_checklist.period_start`, joined on personal_email — never derived from the hire's own dates.** `hr_pending_employees` has no link back to the checklist and its `start_date` is null on 973 of 974 live rows, so the old `start_date ?? created_at` key always degraded to `created_at` (when HR STAGED them, usually the Fri/Sat BEFORE their week) and filed **439 of 954 matched hires — 46% — one week early**; Jul 12 read 169 hires under the old key and 102 under HR's. The cards and the tally MUST read the same key (`batchKeyOf` takes the checklist map) or both become untrustworthy. 52 emails appear under several weeks and none twice in one week, so `pickChecklistWeek` resolves NEAREST the staged week, preferring at-or-after, ties to the later week. The 19 hires matching no checklist row keep their own bucket labelled **"Not on HR's checklist"** — never folded into a real week, never dropped, always counted (`totals.unmatched`); there is deliberately **no name-matching tier** (measured, recovered nothing — see [[hsl-gml-roster-merged]]). **Two reads on purpose:** `/api/manager/pending-hires` answers "what can I action" (3 of the 40 never-attended people — it filters out `promoted` AND every `no_show`, which is why the panel's No-shows section was DEAD UI for its whole life); `/api/manager/orientation-history` answers "who showed up". History must NEVER feed the actionable card list — 975 rows vs ~3. **`hr_new_hire_checklist` is at 1,334 rows, already past the 1000 cap**, so both reads use `selectAllPaged`; the `.range(0, 9999)` style elsewhere in that file silently drops ~334 hires onto the wrong week. The route strips rates and projects the checklist map down to the emails it returns (otherwise a Lead Gen manager receives every hire's personal email company-wide), and the PDF has no money column at all. A history failure CLEARS state, shows a Retry and DISABLES the PDF — never a fallback to the hire's own dates. CSV/Excel (New Hire Check List) still export the current view; the **PDF (Orientation tab) exports the whole history**. **It is an INNER tab on My Team, not a top-level one** — an inner tab inherits the `manager`/`team` grant, while a new top-level feature key is hidden by default (no row == hidden) and would need granting person by person. Both My Team tabs read the history through ONE hook (`useOrientationHistory`) so they cannot disagree on a week or a count. **A history failure degrades the New Hire Check List's week labels** — `checklistWeeks` empties and `batchKeyOf` falls back to the staged week, i.e. the 46%-wrong key — so that tab renders an amber banner naming the failure; remove the banner only if you also remove the fallback. |
| Manager scheduling (Manager -> Scheduling) | `manager-scheduling.md` · `manager-my-team.md` (the no-pay rule) · `hsl-weekend-ot-pay.md` (what pay keys on today) | [[manager-scheduling-ui-first]] | **UI ONLY as of 2026-08-26 — no route, no table, no migration.** The tab renders `src/lib/manager/scheduling-preview.ts` and every edit lives in React state; a refresh discards it, and a permanent banner says so. Delete that file when the route lands. **The unit is a PERIOD, never a field on a person** (`effectiveFrom`/`effectiveTo`, inclusive, null end = current): changing a schedule means closing the current period and opening a new one, because editing in place silently rewrites what past weeks looked like. **`isScheduledDay` returns `boolean | null` and the null is load-bearing** — a date no period covers is "no schedule on file", NOT "scheduled to rest", and only the second excuses an absence; do not simplify it to a boolean. **Shift windows are integer minutes, never text** — `parseShiftWindow` is a REJECTING parser (both halves AM/PM = 12h, neither = 24h, exactly one = refused; zero-length refused), so `8AM-4PM`, `8:00 AM – 4:00 PM` and `08:00-16:00` all key as `480-960`; adding an inference branch to accept `8-4PM` re-opens the split-category bug. **Rest days will be checkable, shift windows will NOT** — `hubstaff_hours` stores a per-day total, `activities/daily` returns `{date,tracked,overall}`, and `user_presence` keeps one `last_seen_at`, so "scheduled Tuesday, no hours Tuesday" is answerable and "started three hours late" is not until a time-entry feed exists. Never build adherence on `shiftWindow` before then. **A missing window is "Hours not set", never 00:00**, counted in its own KPI. **Nothing here feeds pay** — HSL prices days by the CALENDAR (weekend premium, PAB coverage, orphanage OT) and it stays that way; wiring a schedule into a pay rule is a money path under `hardening`, not a refactor. Seeding is the real cost (591 HSL people, 0 scheduled), which is why team defaults lead the panel and `unscheduled` is always counted separately. OPEN before the route ships: a bare `HSL` family grant vs `hsl:<sub_team>` — `normalizeDeptToKey` collapses every `hsl:*` into one key, so family-collapse scoping would show a sub-team manager all 591. |
| Orientation attendance (HR -> New Hire Checklist -> Orientation) | `hr-orientation-attendance.md` · `manager-orientation-attendance.md` · `new-hire-checklist.md` | [[hr-orientation-attendance-tab]] · [[manager-orientation-weekly-attendance]] | **"Listed" and "staged" are different numbers and only staged is a denominator.** HR lists a week in `hr_new_hire_checklist`; a hire becomes markable only once they reach `hr_pending_employees`, and ~9 a week never do (2026-08-23: **79 listed → 70 staged → 66 attended**). A listed-but-unstaged hire can never carry an `orientation_attended_at` stamp, so putting them in the denominator caps the week under 100% forever and reports an intake problem as an attendance problem — they get their own **"Listed but never handed to a manager"** section, and a third bucket (`listedStagedElsewhere`, 2–8/week) holds re-listed hires the model filed under a later week so they are never miscounted as missing. **The rate is `attendanceRate` IMPORTED from the manager model**, never re-derived, so the two surfaces publish one percentage for a week; that carries the ruling that an unmarked hire counts AGAINST the week, which the panel ANNOTATES with a note rather than softening (changing the denominator here makes HR and the manager disagree — the one failure this must not have). **A week that staged nobody is UNMEASURABLE, not 0%**: `measurable` is false, `rate` is null and a note replaces the numbers — every week before 2026-06-07 is permanently in that state (2026-05-03: 52 listed, 0 staged) and a freshly listed current week looks identical (2026-08-30: 31 listed, 0 staged). **One selector, one week**: the panel takes `period` from the host tab and has no week control of its own, and "Hires listed" reads the GRID's own rows — not a second query — so the two inner tabs cannot disagree and adding a hire updates both with no refetch. **One fetch serves every week**: the payload is week-independent and cached under `HR_TAB_CACHE_KEYS.orientationAttendance`, so switching weeks costs no query and returning paints instantly; Refresh writes back through the same key, and the cache is never persisted. It is an **INNER tab** so it inherits the `hr`/`new_hire_checklist` grant (a top-level key would be hidden by default). Rates are stripped UNCONDITIONALLY, both reads use `selectAllPaged` (`hr_new_hire_checklist` is at 1,351 rows), and a failure clears state and offers Retry — there is no fallback to the hire's own dates (the 46%-wrong key). |
| PAB, orphanage & time | `orphanage-pay-step.md` · `orphanage-dispute-flow.md` · `orphanage-pab-coverage.md` · `third-party-vendors.md` · `time-adjustment-requests.md` · `gift-tracker-shipping-export.md` | [[gift-tracker-shipping-export]] · [[pab-payout-week-gate-and-pill]] · [[pab-calendar-parity]] · [[orphanage-pab-auto-coverage]] · [[employee-pab-dispute-removed]] · [[time-adjustments-segments-migration]] · [[time-adjustment-second-approver]] · [[orphanage-pay-ot-differential-underpay]] | **Orphanage PAY and orphanage PAB-forgiveness are two rules** — `orphanage-pay-step.md` owns the money. Pasted orphanage hours price at `min(hours, 40 − already worked) × regular + the rest × OT`, and orphanage OT is the **FULL 1.5× rate, NEVER the weekly 0.5× differential** (orphanage hours have no base leg for a differential to top up). On HSL sheet-form rows the OT rate is **derived** `round2(regular × 1.5)` because `CalcRow.otRate` on those rows IS the differential; every other row keeps its stored OT rate, but an OT rate **below regular is REFUSED, never substituted** — below-regular is the shape of a differential, and quietly deriving would hide bad rate data. The regular leg rounds to **2dp hours** and each leg is priced then summed on sheet-form rows (the Hogan sheet is the payment authority and multiplies 2dp hours), with the OT leg as the **remainder** so the legs still sum to the pasted hours; that is what makes HRIS equal the NPD sheet to the centavo (₱7,224.25, where 4dp hours give ₱7,224.00). ONE implementation, `orphanage-pay-pricing.ts`, shared by the paste tool and the Re-price action so a repair and a re-paste cannot differ. **Two carriers and the blob is the one that PAYS**: `orphanageAmounts` in `payroll.wizard.additions.<file>` is the money, `orphanage_pay` is the first-class record (hours + both rates) and its write on lock-in is best-effort; a **manual cell edit writes only the blob**, so a hand-typed amount has no record, cannot be reconciled or re-priced, and if lost survives only in `audit_log`. Every locked amount is now **reconciled against its own recorded hours on every render** (`reconcileLockedOrphanageAmount`) and `unverifiable` is **not a pass**; Re-price is human-initiated and audited, **never automatic** — a rate may legitimately change after lock-in. Hours on record with no amount on the column get their own panel: that is what a clobbered save looks like. **2026-08 incident**: `e0028b8d` (08-11) made `otRate` the differential, 34 people locked in half-paid on 08-17, `41a21ae1` (08-18) fixed the binding but **re-priced nothing**, a hand re-paste fixed all 34, then ONE manual cell edit from a stale tab wrote its whole map back and reverted them. **OPEN**: the additions blob is last-writer-wins on the WHOLE object (fix = optimistic concurrency on `payroll.wizard.additions.*`, out of scope 2026-08-21), and **9 people are still underpaid ₱27,550.20 + a lost hand-typed ₱5,373.00** on the 2026-08-09 week (unpaid, so recoverable). Detector: `scripts/audit-orphanage-pay-divergence.mts` (read-only) — it reports underpaid and overpaid **separately, never netted**. **Every PAB calendar reads Sun–Sat as of 2026-08-27** (Aliviah's ticket; see `docs/notes/hsl-week-model-cutover-2026-05-31.md` § The 2026-08-27 sweep). The cutover was always live — `hsl.week_model_cutover` is UNSET so the `2026-05-31` code default applies — but **only four call sites resolved it**; five took a legacy default and stayed pre-cutover for three months, worst of all `member-monthly-pay.ts`, which scored HSL PAB Mon→Sun while dispatch paid Sun→Sat. `checkHslPabEligibility`, `getHslAdjustedEnd` and `buildCalendarMonthWeeksIncludingWeekends` now take the week model / anchor as a **REQUIRED** argument — there is no safe default for a value whose right answer changes on a date. **Non-HSL grids moved, non-HSL money did not**: PAB is still won on **Mon–Fri** and the new Sat/Sun cells are `scoring: false`, so **every verdict MUST filter `PabCalendarDay.scoring`** or it fails everyone on two blank cells (identity test: `pab-calendar-sun-sat-display.test.ts`). Pre-cutover months still render Mon→Sun on purpose. **OPEN**: `getPabMonthRange` still defaults Mon→Fri, so a month Accounting does not hand-set reverts (Sept 2026 = Mon Sep 7 → Fri Oct 2). PAB forgiveness = disputes **⊕ time adjustments**, keyed by Hubstaff email. PAB pays only the week containing the period end. Adjustment segments cover **missed** time only. **A time adjustment now needs TWO stage-1 signatures** (2026-08-19): the department manager **and** a second approver the manager names per request. **The pool is that request's OWN TEAM since 2026-08-27** (Kane's ruling; it was previously any department) — "the respective team" is the department of the employee who FILED it, not the union of the manager's departments, resolved server-side from the request id by the SAME resolver the manager's authorization uses so the picker and the guard cannot disagree. `status` is **derived, never written** (`deriveAdjustmentStatus`), so the two can sign in either order; **either denial is terminal** (reusing `manager_denied` so every downstream reader is untouched); approving without naming someone is refused server-side. The second approver's authorization is the **on-row assignment**, strictly **additive** to the manager's `department_managers` check — and `GET /api/manager/time-adjustments` widens the READ by exactly the same rule, returning `managedIds` so a row reaching someone only as second approver never shows the manager's controls. **Naming NOW GRANTS the seat — and ONLY this seat** (2026-08-27, superseding the 2026-08-19 "naming confers no access" ruling): the pool is the roster, not the role table, and the named approver needs no `manager` role. It does not breach the admin-only grant rule because **nothing is written to `employee_roles` or `employee_feature_permissions`** — the capability is derived live from the on-row assignment, exercised in the **employee portal** (they never load the Manager dashboard), and gone when the assignment is. Leaves/transfers/offboarding/resignation/team/HSL stay closed by their own per-tab grants and suspension 403s on zero department assignments. `second_approve`/`second_deny` are the only actions that dropped the `manager:time_adjustments` gate — a NARROWING, since "is the person named on this row" refuses every unnamed manager who previously qualified. Auto-granting a role would have been the wrong shape: it provisions EVERY tab to `edit` and force-logs-out the target. The employee edit-lock now keys on the **decisions**, not the status — a row stays `pending` after the second approver signs. Only `status='approved'` still moves money (`current-pay.ts:273`). **Migration APPLIED 2026-08-20** — all eight columns verified present two ways (the script's `information_schema` read-back and an independent PostgREST probe, which also proves the schema cache reloaded); dual sign-off then proven end-to-end on a live request. **PAB exclusions are audited from 2026-08-20** (`pab_exclusion.added` / `.removed`, see `pab-exclusions.md`) — before that the action that zeroes a month's PAB recorded nothing, while merely approving a dispute was fully logged; the 107 pre-existing person-month entries have no recoverable author and cannot be backfilled. The **Gift Tracker export is MASTER-LIST grain**: submissions are joined on, they never decide membership, so a person with no start date / no milestone / no submission is still a row — a submissions-only file would agree with the tenure-gift Google Sheet **by omission** and hide the exact gap it is built to find. Submitters matching no roster row are **appended flagged `Off-roster`**, never dropped (they are the likeliest mis-ship). Submitted address wins over the master-list home address and `Address Source` always says which. **No price ever** — `gift_price_php`/`gift_name` are vestigial. `listShippingDetails` is **paged**: truncation there does not shorten the export, it blanks addresses on real people. |
| Hubstaff ingest | `hubstaff-weekly-auto-sync.md` · `csv-imports.md` · `hubstaff-zero-hours-gap.md` | [[hubstaff-double-ingest-duplicate-batch]] · [[hubstaff-ingest-blocklist]] · [[hubstaff-weekly-auto-sync]] · [[vano-hubstaff-api-case]] · [[hubstaff-zero-hours-gap]] · [[kpi-calculator-week-unresolved-hang]] | A double ingest must collapse to the **preferred batch** — readers dedupe, they do not sum. **A batch's `source_file` is the ONLY address its pay week has** and 17 modules re-derive the week by parsing it, so an undatable name fails nowhere and degrades everywhere: `replaceHubstaffHoursFromCsvText` now refuses it before writing a row (pattern `YYYY-MM-DD_to_YYYY-MM-DD` **and** a Sunday start — a Monday-anchored name parses fine and strands the same rows; the SPAN is deliberately unchecked, prod carries both 7- and 8-day ranges). Live cost of one bad name, 2026-08-24: both KPI Calculators hung for ~25 min and the week's MESA deposits were skipped outright. |
| Zero-hours reminder (no Hubstaff hours this week) | `hubstaff-zero-hours-gap.md` | [[hubstaff-zero-hours-gap]] · [[catalog-visible-dispatch-absent-means-no-hours]] | **The detector already existed and had the answer — what was missing was DELIVERY.** `Overview.tsx`'s "Hubstaff ↔ Master matches" tile had classified no-hours people (approved leave included) all along and had `jvincec@simple.biz` correctly in its gap bucket while he sat Active with zero hours for two weeks; it is a passive tile nobody opens and its bucket held **228 rows, ~201 of them structural false positives**. Never rebuild the rule: `classifyZeroHours` (`zero-hours-gap.ts`) is the ONE implementation, shared by the tile, the Readiness **No Hours** tab and the `payroll.hours_gap` notification, and its priority — **untracked dept → approved leave → onboarding timing → gap** — is load-bearing. **`isHubstaffExemptDept` must survive a rename**: the list holds base labels and retries once with a trailing parenthetical stripped, because `Site Building` became `Site Building (US - Freelance)` / `(PH - Freelancer)` (33 people, **0** tracked between them) and the exact-match list silently inverted its own meaning. **Lead Gen is TRACKED** (Kane, Q1) so the list runs **~190/week**, which is exactly why the dimension is **LISTED BUT NEVER SCORED** — scoring it would peg readiness near zero every week and kill the 100% confetti, and `ReadinessScoreComponent['key']` is a closed 3-member union whose weights sum to 1.0 (a test named *"the score has exactly three dimensions"* is the tripwire; do not satisfy it by adding a fourth). **Approved leave excuses a week, but `leave_requests` held 4 rows TOTAL (3 approved) at ship** — so a real holiday usually reads as a gap; the list is a prompt to ask, never "absent without leave". **One zero week flags** (Q3 — `jvincec` tapered 39h→33h→9h→0 rather than stopping cleanly). **Accounting only** (Q4). **Never add a cron**: every `/api/cron/*` 401s on the fail-closed `CRON_SECRET` gate (two crons have never run, ever) and the n8n weekly pull was retired 2026-08-20 "by design" — the trigger is the manual Hubstaff ingest, beside `payroll.available`, and `vercel.json` stays untouched. The card carries a **count + top departments, never ~190 names**, sends **nothing** at zero, de-dupes per `(recipient, source_file)`, bails rather than double-notifying if the de-dupe read fails, and audits its own failures via `recordNotifyFailure` — a bare `console.warn` is how `kpi.scored` stayed dead 3 days. |
| Access control & audit | `rbac-feature-permissions.md` · `route-authorization.md` · `delete-authorization.md` · `admin-api-keys.md` | [[subagents-write-to-live-db]] · [[security-invoker-view-silent-empty]] · [[penny-audit-log-visibility]] · [[admin-penny-ai]] | A `security_invoker` view returns a **silent empty set** when any table its filter sub-selects is RLS-blocked. Check every table the filter touches, not just the outer one. |
| Employee team tab (directory · rankings · policies) | `employee-team-directory.md` · `manager-my-team.md` (the no-pay rule) | [[employee-team-directory-rankings-policies]] | The tab is **named after the viewer's own department** — the Pages registry (`visibility.ts`) and `humanizeTabId` deliberately keep the static `team`/"My Team" identity, because both are workspace-wide and cannot be per-viewer. **Rankings show SP + tier, never pesos** (`manager-my-team.md` strips comp from every My Team surface; Kane confirmed 2026-08-14) — `bonus_catalog_applied.amount` is not in the SELECT, and a test pins both the row shape and the projection string. **Rankings are also allow-listed to ONE reader** (Kane, 2026-08-29): `canViewTeamRankings()` in `src/lib/rbac/rankings-viewers.ts` gates the route ABOVE the elevated-role branch, so admin/payroll/finance/hr/viewer sessions get `{ weeks: [] }` too — this route no longer mirrors `/api/team-roster`, and the doc says so. It allow-lists the VIEWER, not the department, so `hasSpRankings()` stays data-driven and a second team on the same bonus shape still lights up (for the allow-list). Denial returns the empty-week shape, so the pill vanishes through the same path as an unscored team; a source-scan test pins the gate ahead of `hasElevatedRole`. `vars.Ranking` is a **tier flag, not a position**: 1 / 25 / 50 / 0 → ₱2,650 / ₱1,325 / ₱530 / ₱0; the displayed #1..#n is derived by sorting SP desc. A week is visible only at `ready`/`locked` (mirrors `getEmployeeKpiResults`) — `draft` is a manager mid-scoring. Policies are a **verbatim, display-only mirror** of the 10 published simple.biz pages and **drive no logic** — the "40 hours" copy is NOT the payroll OT threshold. Only 10 of ~25 roster labels have a page; the rest get a fallback that **omits the workday window and notice period** rather than assert the wrong shift. Returning to the tab must never refetch: `EmployeeApp` keeps it mounted, so every fetch keys on the raw department label. |
| Employee Penny AI (chat bubble) | `employee-penny-ai.md` · `ceo-assistant.md` (the shared widget) | [[employee-penny-ai]] · [[admin-penny-ai]] | **No employee tool takes an identity argument** — the route resolves ONE email via `authorizeEmailAccess` and every tool closes over it, so a prompt-injected "show me Jane's pay" has no parameter to travel through (the CEO/Admin sets take `work_email` precisely because those callers may read anyone). Two tests enforce it: an **allowlist** admitting only `weeks` as a tool input, and a source scan proving the bodies read nothing else off the model's `input`. Schemas live in `employee-tool-defs.ts` **without** `server-only` so those guards can run outside Next. Penny **reports figures, never recomputes them** — pay via `runCeoTool('get_employee_pay')` with the email pinned (inheriting the live-`payment_dispatches` freshness overlay), rate/team/bonus-qualification via `resolveCoeFacts` (the COE resolver) — so a week it can't find is a week it declines to price, pointing at Pay Stubs instead. It **never judges PAB eligibility** (that verdict needs daily hours ⊕ disputes ⊕ adjustments ⊕ holidays; a second implementation would contradict the Overview PAB calendar) — it states amount + window + rule + `isFinalPabWeek` pay week and sends them to the calendar. Windows are **asked for, never derived**: PAB via the dashboard's own `resolvePabMonthForDate`→`resolvePabRangeForMonth` pair, Tech via `resolveIsTechBonusWeek`. **Policy answers are verbatim-only**: `has_team_page: false` means the workday window and time-off notice period are absent ON PURPOSE, so Penny names them unpublished and points at the manager — a test pins that instruction in the tool description, because without it an LLM supplies a plausible shift time. **The 10/day allowance is a COUNT of `penny_employee_usage` rows since Manila midnight** (fixed +08:00), never a counter column (a lost update = a free prompt) and never `audit_log` (admin-truncatable ⇒ would refund the company); reserve-before-the-model-call, then `refunded_at` when a turn produced no text, so an error never costs one of the ten. Every read **fails CLOSED** (missing table/client/error ⇒ spent out), which is why Penny is correctly unavailable until the migration is applied. **The client never counts** — it renders `GET /quota` + the `X-Penny-Quota` header, erring toward showing FEWER left. At zero the bubble **greys, never vanishes** (warn at 2–3, "last question" at 1), one ladder in `quotaFromUsed` so pill and composer can't disagree. Elevated `?email=` viewers answer about `subject_email` but charge `session_email` and are **exempt** — count by session, never subject. Haiku 4.5 is an **older-generation** model: `output_config.effort` **errors** and adaptive `thinking` is the wrong shape — do NOT copy the Admin route's generation config. System prompt is split cached/uncached so the clock doesn't invalidate the prefix (the siblings' bug). **Replies are RENDERED, not printed** (2026-08-19): the original “no Markdown, plain text only” bet holds for Sonnet and loses for Haiku — employees saw raw `**` and rows of `***`, so the fix was the RENDERER, never a firmer prompt (`src/lib/penny/chat-markdown.ts` parses bold/italic/strike/code/bullets/numbered lists/headings/`***` rules; `ceo-chat-message.tsx` maps them to React elements). **Layer order is the safety property**: biz-report fences and pipe tables are consumed BEFORE the Markdown pass, so report JSON is never reformatted — pinned by a test asserting the block parser has EXACTLY ONE call site and that table cells get the INLINE pass only (block-parsing a `-` cell turns Penny's empty-value marker into a bullet list). No raw-HTML path: the parser returns data, the renderer returns elements, and a guard test scans both files for React's escape hatch by name. Every parser rule traces to a real failure — a delimiter followed by whitespace cannot OPEN emphasis (`Rate * hours = pay. 2 ** 3` italicised everything between two unrelated strays; **caught by server-rendering a real reply, not by the tests**), `_` emphasis only at word boundaries (`work_email` would italicise its middle), unmatched delimiters stay literal (a mid-stream `…**` must not swallow later words — a test walks EVERY prefix), longest delimiter first (`***` before `**` before `*`). **CEO/Admin prompts deliberately unchanged** — the renderer is additive for them; rewriting their prompts to use bold would restyle two live surfaces nobody asked about. **Self-service guides are DATA, tested against the UI** (2026-08-19): `get_company_how_to_guides` returns the COE / pay-stub / leave procedures from `src/lib/penny/employee-guides.ts`, and tests assert every label it names still exists in the component that owns it (Profile tabs, leave sub-tabs + all 5 leave types, `documents/types.ts` labels, `Request certificate`/`Submit request`/`Signed document` + the period options) — **a guide naming a button that no longer exists is worse than no guide.** It carries the counter-intuitive facts on purpose: a COE has **nothing to attach** (the HRIS writes it, hence *Request certificate*), a COE **can be refused** 422 on a missing start date/dept/rate, "pay stubs" means **two different things** (instant Profile → Pay Stubs vs. the Accounting-signed copy via Request Documents), and reconstructed weeks are **estimates** missing performance bonuses. **The time-off notice period is PER-TEAM and Penny may never invent one** — measured 2026-08-19: 10 teams publish it (one week ×9, **two weeks for AI/Automation**) and the company-wide fallback carries **no attendance policy at all**. The leave guide quotes the team sentence VERBATIM (never parsed to a number — it also carries the attendance-bonus condition) and otherwise says it is unpublished and points at the manager, per `employee-team-directory.md:176`. The value is folded into this tool rather than left to a second `get_company_policies` call, because a forgotten chain is exactly how a made-up notice period reaches an employee. **The leave form enforces NO notice period** (`min={today}` + `end ≥ start` only) — a test pins the guide saying so. Schema and dispatch live in two files, so a test asserts every declared tool has a `case` and vice versa (a rename silently becomes "Unknown tool" and costs a real prompt). **Accounting's payment `status` NEVER reaches an employee** (2026-08-19): `ceo-tools.ts` defines "pending = owed but not yet sent", which mislabelled weeks 7–9 weeks past — measured read-only + paged, ~2,900 `disbursement_records` across 2026-06-21/06-28/07-05 carry **zero paid dispatches**, and 07-12 shows 732 pending vs 330 paid (the gap already open in [[never-paid-and-misdelivered-paystubs]] item 3 since 2026-08-07: *paid outside HRIS, or never written?*). **Both obvious fixes are lies about the same missing flag** — pending→paid invents a payment, "owed but not yet sent" asserts a non-payment. `src/lib/penny/pay-status.ts` translates to five states (`paid` · `scheduled` · `processing` ≤4d grace · `not_recorded` · `on_hold`) and the raw `status` key is **DELETED** from the payload, never renamed alongside (two vocabularies = the model picks the wrong one). A real `paid_at` **outranks the status column** (the column is the unreliable half); an underivable pay date falls to `not_recorded`, never `scheduled`. `not_recorded` states the absence and **explicitly refuses to read it as non-payment**, routing to Accounting — matching `paystub/route.ts:47-59`, which deliberately does not gate stubs on a paid dispatch because that "would hide most (or all) of an employee's weeks". **CEO/Admin keep the raw status** (Accounting needs ground truth incl. its holes) and `disbursement_records` is untouched — closing the real gap needs Kane's answer, not code. Probe: `scripts/audit-employee-pay-status.mts`. The employee bubble uses `/Chatbubblev2.png` (headset heart) via `markSrc`; CEO/Admin keep `/chatbubble.png`. **Penny speaks first after 5s on the employee Overview** (2026-08-19) — a speech balloon off the chat head with 3 FAQ chips. **A proactive offer is a PROMISE**, so `src/lib/penny/employee-faq.ts` names the tool answering each entry and a test asserts that tool exists in `EMPLOYEE_TOOLS`; another forbids leave balances (untracked), peer pay (unreachable), and "did I earn the PAB?" (the tool deliberately does not judge eligibility). An unfulfillable offer costs the employee one of their ten prompts to hear "I can't". The FAQ pool doubles as the panel's empty-state chips — **ONE list**, or the volunteered and the offered drift apart. **Every reason to stay quiet lives in the render-time `showGreeting` expression**, not in the effects: panel open · conversation exists · widget hidden · **allowance spent** (inviting a question they have no prompt for is worse than silence). The 5s fuse is armed once and deliberately NOT gated on `open`/`messages` — a timer that long outlives them, so gating it there is a stale closure either way; **add new suppression reasons to the render guard.** Opening Penny dismisses it for the session (`sessionStorage`, per identity, so an elevated `?email=` viewer is not re-greeted per employee); it also self-retreats ~22s. **The bubble is on EVERY employee tab** (2026-08-20, superseding the 2026-08-19 "Overview - Chat Bubble only"): mounted OUTSIDE `EmployeeApp`'s tab loop, so it is ONE persistent instance and the transcript, quota pill and greeting dismissal survive a tab hop (per-tab mounting would reset the conversation whenever someone checked their hours mid-question). It still follows the Overview **page** — `visibilityOf('employee','dashboard') !== 'hidden'` — because Penny reads that page's pay figures aloud and a read surface must not outlive the switch that turned the page off; hiding any other page does nothing to it. **The greeting balloon did NOT spread**: `hostQuiet` is a new, REQUIRED suppression reason in the render-time `shouldShowGreeting` predicate (never a gate on the 5s timer — that fuse outlives a tab switch), because a balloon volunteering questions over a half-typed leave request or a bank-details field is an interruption, not an offer; it is a gate not a latch, so returning to Overview inside the ~22s window still shows it. Safe to spread because MEASURED: no employee tab has its own fixed bottom-right control (`grep 'fixed bottom|fixed right'` over `src/components/employee/` returns nothing). Two source-scan guards in `employee-faq.test.ts` pin both halves. |
| Accounting surfaces | `accounting-total-payout.md` · `accounting-cobrowse.md` · `urgent-payments.md` · `tickets-board.md` · `system-diagnostics.md` · `manager-my-team.md` · `ceo-assistant.md` · `login-carla-song.md` | [[accounting-transfers-kpi-search-export]] · [[overview-total-payout-salary-only]] · [[cobrowse-observe-two-bugs]] · [[tickets-board-deploy-steps]] · [[tickets-update-notifications]] · [[people-pay-oneoff-urgent]] · [[contractor-invoices-hidden-by-period-filter]] · [[carla-signin-song]] · [[pd-focus-mode-retract-fallback]] · [[tickets-dedicated-role-only]] · [[tickets-black-red-theme]] · [[payroll-exports-itemized]] · [[overview-tech-eligibility-wizard-parity]] · [[overview-roster-table]] | Payment Dispatch is **hard-scoped to the cycle window** by deliberate decision — contractor rows open the invoice instead. Every /tickets notification picks its recipient **in code** (`recipients.ts`), never in the n8n workflow. |
| People tab roster export (PDF · XLSX · CSV) | `people-roster-export.md` | [[people-export-account-last4-and-updated]] · [[people-tab-bank-drift-audit]] · [[bank-preferred-field]] · [[postgrest-1000-cap-sweep]] · [[hurupay-kolan-rebrand]] | Exports **the roster in view** — search, department, OT toggle and the selected week/range all apply — fully client-side. **CSV and XLSX share ONE column list** (`FLAT_COLUMNS`, each entry carrying its own Excel width, so there is no parallel array to skew); the **PDF has its own narrower set** and is the artifact that can silently lose a column name — `drawHeader` used to render only the FIRST wrapped line of a header, which is how `Last 4` first shipped as nothing at all. It now draws every line and a test pins all ten labels. **The account column is LAST-4 ONLY and is masked on the SERVER** (`people-roster.ts`), so the browser never holds a full number — Kane's 2026-08-12 cycle-close ruling, reusing that module's `maskAccountLast4` rather than a second copy. It is **slot-aware** via `resolvePreferredAccountNumber`: 8 people sit on the alternative slot with a different alt number and two of them have ONLY an alt, so reading `account_number` directly prints an account the payment is not going to. No legacy rates-sheet fallback — PD backfills wallet EMAILS from that row, never account numbers. A **wallet-rail payee's stale bank account is still printed** by decision: the adjacent Payout Method column names the rail, and blanking it would make "no account" and "wallet-routed" indistinguishable. **"Bank Info Updated" reads `bank_update_history`, never `bank_last_self_updated_at`** — the stamp is written by 3 of the 6 routes that change payout details, the history table by all 6; measured 2026-08-24, all 740 stamped people have a history row and the stamp is never newer. That read is **paged** (1,334 rows — a bare select stops at 1000 and the tail reads as "never changed"), and its failure is non-fatal but **surfaced as a warning banner**, because an empty map silently means "nobody ever changed their bank". Seed/repair scripts write `employee_ids` with no history row, so a seeded bank shows blank. **OPEN:** the Payout Method column still prints "Hurupay" for Kolan payees — the old name is correct only on historical ledger records. |
| PAB exclusions (zeroing one month's PAB) | `pab-exclusions.md` | [[pab-calendar-parity]] | An exclusion sets one person's PAB to **₱0 for a whole month** regardless of attendance. Stored as a **JSON string** in `app_settings.pab_period_exclusions`, keyed `YYYY-MM` → array of emails — `JSON.parse` before counting, or `Array.isArray` reports 0 on a populated setting (measured: 2 months, 107 person-month entries). **Every change writes `pab_exclusion.added` / `.removed`** via `getSessionActor()` + `insertAuditLog`, mirroring `pab_dispute.*` so existing audit readers and the Penny action-family search need no changes — and **only when `changed`**, since re-posting the same state is not an event. The 107 pre-existing entries have no author and **cannot be backfilled**; the trail starts 2026-08-20. The employee notification (`pab.excluded` / `pab.restored`) is **best-effort and must stay non-fatal** — the exclusion is already written by then — but it is no longer silent: failures go to `audit_log` as `notification.insert_failed`. These two types were **dead 17 days** behind a `console.error` with 0 rows each. |
| Notification alerts (chime + toast) | `notification-alerts.md` | [[notification-alerts-view-scoped]] | **Every `useNotificationChime` mount passes a `view`.** `GET /api/employee-notifications` treats an absent `view` as *every type the viewer may see*, so an unscoped alert rings on the wrong dashboard for a dual-role account — that is how **HR heard about money**: bank changes fan out to admin/accounting/ceo only and `people.banking.self_updated` is mapped away from HR, yet an HR coordinator also holding accounting/ceo got the payout chime on the HR dashboard while HR's panel correctly hid it (Kane, 2026-08-17: *HR should not hear about bank changes*). The money rule lives in the **type→view map**, not in the alert, and a test pins both halves — hidden from HR **and** still reaching Accounting, plus any future `people.banking.*` / `bank_preferred.*` / `bank_info.*` type mapped to `hr` fails. The high-water mark is per **(email, view)** (`notif-chime-hw:<email>:<view>`): a shared mark lets an Accounting alert push the mark past an older HR notification that never rang, silencing it forever with no error — the legacy unscoped key stays as a read-fallback so the rename never replays a backlog. Accounting announced **nothing at all** until 2026-08-17 (hook was HR-only since written); HR, Accounting and **Employee** announce today (Employee mount added 2026-08-17 with [[kpi-scored-notification]] — announces every employee-view type), and unmapped types deliberately announce everywhere rather than vanish. |
| KPI Scored notification (employee toast + live update) | `kpi-scored-notification.md` | [[kpi-scored-notification]] | **The de-dupe key is the AMOUNT, never "already notified once"** (Kane 2026-08-17: a disputed/re-ordered bonus must re-announce): every trigger recomputes the dept-week's per-person visible totals and diffs against the `details.amount` of each person's latest `kpi.scored` row — unchanged is silent, first-time-zero is silent, any change (up, down, to zero) notifies with `previous_amount`. **Drafts are structurally silent**, which is the only reason hooking the autosave-backed bonus routes (`bonus-catalog-applied`, `hsl-bonus/entries`) is safe: the helper exits unless `hsl_bonus_period_status` is ready/locked, mirroring the employee visibility gate. Every notify call is best-effort — it never fails the save. Policy lives in two pure tested functions (`planKpiScoredInserts`); do not simplify to notify-once. **An absent employee KPI Bonus card is a WORKFLOW gap, never unshipped code** — the card is on `main` (`EmployeeDashboard.tsx:3097`) gated `kpiBonusAmount > 0`, and that value reads only ready/locked weeks, so a scored-but-never-submitted week correctly shows ₱0 (All Time is always ₱0). Check the week, then the status row, then rates — then code. Toast latency ≤ ~30s by design (anon `postgres_changes` never delivers; the chime's poll is the transport, and EmployeeKpiResults polls the same beat). **DDL APPLIED 2026-08-20** (it was DEAD for its first 3 days — CHECK rejected every insert, all call sites swallowed it, 0 rows vs 3,694 for `payroll.available`). **A period FLOOR now gates it to the current pay cycle only** (Kane 2026-08-20) — `isWithinCurrentPayCycle` keyed on **period_end** (the table holds weekly AND monthly grains, so a period_start floor would silence a current monthly period), floored at `payrollNotesWeekStart()`. It is a SEPARATE gate: flooring by weakening the amount diff is forbidden. Notify failures are no longer invisible — every call site routes through `recordNotifyFailure` → `audit_log` action `notification.insert_failed`, still non-fatal. |
| UI, theming & build | `docs/design/ui-standards.md` · `docs/design/responsive-design.md` · `docs/design/orphanage-dashboard-standards.md` | [[collab-chrome-stacking-and-mirror-css]] · [[dashboard-switch-performance]] · [[nextjs-build-vs-dev-shared-dir]] · [[next-themes-script-warning-patch]] · [[dialog-content-no-height-cap]] | `next build` and a running `next dev` share `.next/` — check for a live dev server before building. Portaled surfaces need their theme class applied explicitly. A `p-0` dialog **must** add `gap-0` and a `dvh` height cap — the shared `DialogContent` has neither, so a tall modal clips at both ends and its footer buttons go unreachable. |
| Git & session process | *(no doc — memory only)* | [[do-not-push-user-handles-git]] · [[multi-session-shared-checkout]] · [[worktree-baseref-origin-lag]] · [[subagents-write-to-live-db]] · [[ship-direct-no-pr]] | Commit direct to `main`, **never push**. Stage specific files only — another session shares this checkout. `.env.local` is **production** service-role. **A concurrent session's transcript is a SNAPSHOT, not an outcome**: the last line of a still-running `.jsonl` is where it had got to, not where it stopped — compare the tail timestamp to the clock, and say "in flight" rather than "nothing shipped" (proved 2026-08-25, a session logged as ended committed 40 SP eleven minutes later). |
| Migrations & deploy state | `docs/audits/2026-08-11-pending-migrations-probe.md` · `scripts/audit-pending-migrations.mts` | [[migration-pending-claims-are-folklore]] · [[security-invoker-view-silent-empty]] · [[postgrest-head-true-hides-missing-table]] · [[migration-apply-needs-database-url]] | There is **no migration runner and no applied-ledger**, so every `PENDING` line in `docs/` or memory is a *claim*, never a fact — 21 of 25 were already applied when first measured. **Run the probe before writing, saying, or repeating that any migration is pending**, and correct the doc AND the memory entry in the same commit when it comes back APPLIED. A CHECK constraint's allowed values are **not** readable through PostgREST: that is `UNVERIFIED`, never "pending". **THE PROBE WAS WRONG FOR TABLES AND IS FIXED (2026-08-26).** Found 2026-08-20: `probeTable()` used `select('*', {head:true})` and treated a falsy error as APPLIED, but PostgREST returns **no error at all** and `count:null` for a table that does not exist — so a MISSING table read as APPLIED. Proven on `penny_employee_usage` and on a control table named `definitely_not_a_table_xyz`, while an existing table returns a real count, which is why it hid. `probeColumn()` failed differently: a missing column errors with `code: undefined` and an empty message, landing INCONCLUSIVE instead of NOT APPLIED. Two more sites read a null count as a legitimate zero — the MESA opt-in cleanup would have closed on `zero opt_in rows remain` for a table that might not exist, and the `active_employees` definer probe would have blamed `security_invoker` for a count that never resolved. **The rule is unchanged and now enforced: probe existence WITHOUT `head:true`** (plain `.limit(1)` errors correctly). Classification lives in `src/lib/db/probe-verdict.ts` (7 tests, each confirmed failing against the old logic first); `head:true` survives ONLY for counts, always behind `readCount`, which returns `null` rather than `0` when the count did not resolve. **A NEGATIVE CONTROL now runs first and ABORTS the whole audit** if a table and a column that cannot exist are not both reported missing — a probe that cannot detect absence produces a report indistinguishable from one that found none, which is exactly how this survived. Post-fix run against production: **APPLIED 25 · NOT APPLIED 0 · INCONCLUSIVE 0**, control correctly MISSING both ways. **Running an apply script: `DATABASE_URL` must be the SESSION POOLER** — `postgresql://postgres.<ref>:<pw>@aws-1-us-east-2.pooler.supabase.com:5432/postgres`. The script's own "not the pooler" comment is **wrong for this project**: the direct `db.<ref>.supabase.co` host is IPv6-only and returns no address at all. The user is `postgres.<ref>`, not bare `postgres`, and **an `@` in the password must be `%40`** or the driver truncates the password at the first `@` and misreads the rest as the hostname — which reports as "password authentication failed" and sends you resetting a password that was fine. Session mode on 5432 runs DDL; only transaction mode (6543) cannot. This cost Employee Penny AI and time-adjustment dual approval ~10 days dead in production; both applied and verified 2026-08-20. And the folklore rule runs **both ways** — two of three `PENDING` memory lines measured TRUE on 2026-08-20 (Employee Penny AI and the time-adjustment second approver were genuinely un-run), so *measure*, never assume in either direction. |
| Monday board & sprint tracking | `monday-board-sync.md` · `.claude/skills/monday-board-sync/SKILL.md` | [[monday-hris-board-sync]] | Items are matched **by exact name** — no trim, no case fold, no unicode normalisation. A name that differs by one character (angle brackets are stripped by Monday, em-dashes/curly quotes/`₱` are load-bearing) becomes a permanent duplicate row. `hris-plan.ts` owns existence + structure; the skill owns Status / Actual SP / Completed Date and **never** writes a relation or the Projects Portfolio board. `done: true` is a claim of shipped-and-proven — **Done needs proof, and bonuses ride on it**. **A sprint label asserts a DATE RANGE**: windows come from the board group titles into `TASK_SPRINT_WINDOWS`, `selfcheck()` refuses a Completed Date outside its sprint's `taskSprintAttribution()` range *and* any date that is not the last sha's commit date (`dateBasis: 'external'` is the only exemption and must name a confirmation). **What re-files a row is the GROUP, not the label** — `sync.ts` moves it via `move_item_to_group` from the update path; epics are still not moved between quarter groups. **Unless the row is `groupPinned`**: a human triage lane ("For Re-scoping") has no Sprint label, so reconciling group-to-label would erase the triage — pinned rows keep their group and the suppressed move is reported as `tasksGroupPinned`, never hidden. Sprint label indices are the board's own and **NOT sequential** (S22 = 3, S23 = 4, S19-21 = 10-12, S27 = 103) — read `settings_str`, never guess; mirror a sprint's **WINDOW** too or its neighbour silently absorbs the gap. **A 429 is a full-minute WINDOW, not a pause** — short backoff burns every retry inside it and kills a 200-call reconcile mid-structure. **Rolling into a sprint moves only OPEN rows** — the Backlog group was 21/22 Done, and pulling that history into the live sprint would credit months of work to a two-week window; **scheduling a row is not promoting it**, so a Pending Deploy row keeps that status rather than being written back to Ready to Start. **Approval binds both the proposal AND the source**: `review.mts` lists and hashes every sprint move, and an `inputsHash` over `{passDate, PLAN_TASKS, ROWS}` makes `apply.mts` refuse a proposal minted before the plan changed — `proposal.json` is one file in a shared checkout, so check its mtime before proposing — but **mtime alone is not the test**: recent mtimes with a CLEAN tree are the previous pass's residue, not a live session. **AUTHOR DATE IS NOT LANDING DATE** — `git log --date=short` prints the author date, so a long-lived branch merged this week reads as work from whenever it was written, and `selfcheck()` **accepts** it because that date is a real commit date; for work that landed via a merge the row's **last sha must be the MERGE commit** (`4a15db2c`, merged Aug 19, branch authored Aug 3). **Budget the verify, not just the write** — a 28-row pass exhausted the whole UTC-day budget and stranded `verify.mts`, so spot-check with `verify-one.mts` between phase 2 and the full verify, or split across two days. Phase 2's post-create re-read proves the rows EXIST and were adopted by name (no duplicates), but **not** the values written. **The SESSION TRANSCRIPTS are a third evidence source** — when a commit's file list is wider than its message, read `~/.claude/projects/<project>/*.jsonl` for that day: `cd681cf8` was titled "Offboarded", held **no offboarding code**, and carried two unrelated features written in two different sessions; file overlap flags the mixture but only the transcript names which is which, and a message-clustered pass loses the smaller row's SP. |
| Skills & doc process | `CLAUDE.md` (repo root) · `.claude/skills/blueprint/SKILL.md` · `.claude/skills/hardening/SKILL.md` · `.claude/skills/monday-board-sync/SKILL.md` | [[blueprint-skill-plan-gate]] · [[hardening-skill-and-open-gaps]] · [[memory-index-load-cap]] | Routing is by **does it exist yet**: a new surface goes through `blueprint` (brief → approval → build → self-document), an existing one through `hardening`. Both read this file at step 1, so **a new doc or memory entry means a new cell here, same commit** — an unmapped entry is invisible to both skills. **`MEMORY.md` is TRUNCATED on load past ~24.4 KB** and says nothing about it, so entries below the cut are invisible to the session: keep index hooks under ~150 chars with the detail in the topic file, and re-measure after adding entries. `docs/README.md` and this file are **both** required — INDEX is what the skills read, README is the human front door; a doc in one and not the other is half-published (22 were, fixed 2026-08-26). |
