# Audit findings — Termination Docs build round 1

## Termination Docs — G1 (personal email SEARCHES, work email IDENTIFIES) and G3 (an active person can never be terminated on paper)

**Verdict: FAIL**

### [BLOCKER] src/lib/documents/termination/termination-facts.ts:116

**What:** G3 FAILS OPEN. `gmlActive: gml.map.get(norm)?.active === true` treats a FAILED or incomplete `fetchGmlStatusMap()` read as "not active", and `arbitrateTerminationFacts` never consults the unstamped rows it already holds in `masterRows`. The absolute refusal is therefore conditional on one fallible read that has no fail-closed handling — the map error is only pushed onto `degraded` (line 107) and generation is never blocked on it.

**Failure scenario:** Input: a WORKING employee who carries (a) one unstamped `global_master_list` row on the current upload and (b) one stamped duplicate row whose `off_boarded_reason` is in the allowlist (a pre-rehire `resigned`, or a leaver's stamp landed on the wrong duplicate), with the master `Start Date` earlier than the stamp — the shape memory `rehire-invisible-offboard-reuse` describes. Trigger: `fetchGmlStatusMap()` returns `{ map: new Map(), error: <msg> }` on any PostgREST error (statement timeout on a multi-thousand-row paged scan is the realistic one), OR it returns `active:false` with `error:null` because its paged read at src/lib/roster/gml-status.ts:48-58 calls `.range()` with NO `.order()` — an unordered multi-page scan over a table the master sync is concurrently UPDATEing can skip a row, and if the skipped row is that person's only unstamped one, pass 1 never marks them active and pass 2 stamps them offboarded. Walk of `arbitrateTerminationFacts`: 1 no_master passes (rows exist); 2 ambiguous passes (one name); 3 still_active NOT taken because `input.gmlActive === false` (termination-arbitration.ts:445); 4 records non-empty from the stamped row; 5/6 reason is allowlisted; 7 rehire passes (stamp > start date); 8 name composes. Result: full facts for an actively-employed person. POST /api/accounting/documents/termination re-resolves and gets the same answer, so it renders, signs with the rep's live signature, uploads, and inserts a permanent log row: a signed termination letter for a working employee. Amplification on the same input: with `write_back: true` and a rep-filled date (the stamped date failed sanity), `applyTerminationWriteBack` keys on `merged.identity.masterRowId` = the winner, which the promotion rule deliberately picks as the CURRENT-UPLOAD row — i.e. the live unstamped row — whose `off_boarded_at` is null, so the blank-only guard passes and the write actually offboards the working employee in the roster. The rep's only signal is an amber "Read partially degraded" note (TerminationDocsPanel.tsx:1079) whose copy talks about blank facts, not about the active check, and only in the error variant — the row-skip variant emits nothing at all. No test covers this shape: every `gmlActive:false` fixture in termination-facts.test.ts also has no unstamped row (lines 316, 430, 440).

**Proposed fix:** Make G3 answer from evidence the resolver already has, and fail closed on the read: (1) in `arbitrateTerminationFacts` step 3, refuse on `input.gmlActive || input.masterRows.some(r => !trimOrNull(r.offBoardedAtRaw))` — this is fetchGmlStatusMap's own polarity restricted to rows carrying this work email, so it is a strict subset of the map's verdict and can never add a refusal a healthy map would not already make; (2) pass the map's error into the arbitration and return a hard `blocked` (e.g. `evidence_read_failed`, or a new `active_check_failed`) when it is set — 'the active check could not run' must never render as a facts sheet; (3) mirror both in `buildTerminationCandidates` (termination-arbitration.ts:224-236), where a failed map currently makes every candidate render as issuable with `blockedCode: null` instead of greyed `still_active`; (4) add `.order('"Work Email"', { ascending: true })` (or key-set paging) to the paged read in src/lib/roster/gml-status.ts:48-58, since an unordered `.range()` scan can drop the one row that proves someone is active; (5) add the missing fixture: masterRows = [stamped 'resigned' row, unstamped row], `gmlActive: false` ⇒ `blocked.code === 'still_active'`.

### [BLOCKER] src/lib/documents/termination/termination-rates.ts:124

**What:** G1 BREACH — a printed money fact is derived from a PERSONAL email. `resolveTerminationRates` builds `aliases` from `arbitration.rateContext.aliases`, which termination-arbitration.ts:641-646 populates with the master row's `personalEmail`, then queries `hr_pending_employees.personal_email` for every alias (lines 164-171) and falls back to alias-keyed `employee_rate_history` rows (`preferWorkEmailRows`, line 358-360, returns ALL alias rows when the work email has none). The resulting figure is printed on the signed page (termination-document.ts:546-561) as the STARTING RATE. G1 states no code path may derive off-board evidence, a rate, a paystub or a printed fact from a personal email; the module header at lines 160-163 concedes the path exists rather than closing it.

**Failure scenario:** Input: generate a letter for `carlath@simple.biz` (resigned 2026-06-03) — the documented cross-wire, whose `Personal Email` is `carlathomas0112@gmail.com`, shared with the ACTIVE `carla@simple.biz` (offboard-evidence.ts:41-48). `carlath@` is a pre-pipeline hire, so hr_pending pass 1 (`work_email ilike 'carlath@simple.biz'`) returns nothing; pass 2+ then run `personal_email ilike 'carlathomas0112@gmail.com'` and hit CARLA's staged hire row, and `carriedRate(...)` returns it with `source: 'hr_pending'`. The signed PDF for carlath@ prints the active employee carla@'s hire rate as carlath@'s starting rate, and the log row and its snapshot record it permanently. The rep cannot detect it: the panel renders `hr_pending` as "the digital hire record" (TerminationDocsPanel.tsx:155) with no hint the row was matched on a shared inbox. Same class via the second carrier: `.in('employee_email', aliases)` (line 129) plus `preferWorkEmailRows`'s "every alias row otherwise" fallback prints a rate-history row keyed on the shared gmail whenever the work email has no history row — and the sheet-era history was written with whatever address the sheet held. No test exercises the alias path (termination-facts.test.ts's cross-wire tests stop at the facts arbitration and never reach rate resolution).

**Proposed fix:** Drop the personal email from the rate identity entirely: in termination-arbitration.ts:641-646 build `aliases` from the work email plus `alternateWorkEmail`/`alternateWorkEmail2` only (the personal address stays on `TerminationIdentity.personalEmail` for the log row, where nothing prints it), and delete the `hr_pending_employees.personal_email` passes over non-work aliases in termination-rates.ts:164-171. If a personal-inbox match is genuinely wanted for pre-pipeline hires, it must not silently become a printed figure: return it as a BLANK with a new `blankReason` (e.g. `matched_on_shared_inbox`) so the rep types the number, or require that the matched hr_pending row's `work_email` normalises to THIS work email before the rate is accepted. Also narrow `preferWorkEmailRows` (line 358) to return `[]` rather than every alias row when the work email owns none. Then extend the G1 grep test in termination-facts.test.ts:515-530 to assert that `src/lib/documents/termination/termination-rates.ts` contains no `personal_email` column reference.

### [MINOR] src/lib/documents/termination/termination-arbitration.ts:445

**What:** G3's polarity, copied correctly, refuses a large share of this feature's real subjects — the still_active arm fires for any leaver who still carries an unstamped master row anywhere, which is the NORMAL shape of a recent leaver. Fail-safe in direction, but it makes the refusal the common path, which is what makes finding 1 dangerous in practice.

**Failure scenario:** Input: any of the 294 people measured as off-boarded on 2026-08-21 while still holding an unstamped current-upload `global_master_list` row (offboard-evidence.ts:8-11 — 1,287 active rows, ZERO carrying `off_boarded_at`). `fetchGmlStatusMap` marks them `active: true` from that unstamped row, so `resolveTerminationFacts` returns `blocked.code === 'still_active'` and the tab refuses to issue a letter for a genuine, fully-processed leaver — indefinitely, since the unstamped row survives in the table after the sheet drops it from the current upload. Wrong outcome: the rep is told a resigned person is ACTIVE, the letter cannot be generated, and the natural workaround is to get someone to stamp the live master row by hand — a roster write made to satisfy a document.

**Proposed fix:** Keep the refusal (it is the correct direction) but make it actionable rather than a dead end: state in `still_active`'s message WHICH row is unstamped (`candidateRowIds` already carries the ids — surface the unstamped one and its upload) so the rep can hand HR a precise fix, and consider scoping the active test to rows carrying this WORK email (the map keys all four email columns, so a shared personal inbox on someone else's live row currently blocks a real leaver too). Do not relax it to a first-match `rows.find(r => r.off_boarded_at)` check.

## Termination Docs — G2 (temporary_pause), G4 (rehire guard), G5 (no impossible date on a signed page)

**Verdict: PARTIAL**

### [MAJOR] C:/Users/Kane/Desktop/simple-hris/src/lib/documents/termination/termination-arbitration.ts:548

**What:** G5 DEFEATED: sanitizeOffboardDay(normalizeMasterDate(x)) is correctly ORDERED everywhere but is not sufficient — normalizeMasterDate's `new Date(s)` fallback (master-date.ts:46) FABRICATES a missing year and a missing day-of-month, and sanitizeOffboardDay cannot see it because the result is a well-formed past ISO day. A partial 'Start Date' cell therefore prints as a precise calendar day on the signed letter, and the rep cannot correct it.

**Failure scenario:** `global_master_list."Start Date"` is a TEXT column (references/sql/create/supabase_global_master_list.sql:19) holding free text off the Google Sheet. Cell = "Aug-24" (a plausible spelling of August 2024) -> normalizeMasterDate -> '2001-08-24' -> sanitizeOffboardDay accepts it (it is in the past) -> facts.startDate='2001-08-24', facts.startDateLabel='August 24, 2001' (termination-arbitration.ts:548, :618) -> termination-document.ts:539-540 prints the leader row `Contract start date .......... August 24, 2001` on the signed PDF, and termination-log.ts stores start_date='2001-08-24'. Because startDate is non-null, computeTerminationBlanks does NOT list 'start_date', so route.ts:194 rejects any rep correction with 400 "start_date was resolved from the record and cannot be supplied by hand" — the rep is forced to issue the letter with a 25-year-wrong engagement date or not issue it at all. Same class, all verified by running verbatim copies of both functions: "August 2024"->2024-08-01 (fabricated day-of-month), "2024 August"->2024-08-01, "March2024"->2024-03-01, "2024?"->2024-01-01, "2026"->2026-01-01, "0"->2000-01-01, "Aug 18"->2001-08-18. G4's comparison and the DDL `termination_date > start_date` both pass happily on the fabricated value.

**Proposed fix:** Do NOT loosen sanitizeOffboardDay. Add a third, positive step between normalize and any printed fact: accept only `/^\d{4}-\d{2}-\d{2}$/` AND a real-calendar-day round-trip (new Date(y, m-1, d) must read back the same y/m/d) AND a plausibility floor (e.g. >= 2015-01-01). Anything else becomes a BLANK with blankReason 'date_failed_sanity' plus the existing degraded note, so the rep is ASKED for the start date instead of the letter inventing one. Apply it in the arbitration `day()` helper (termination-arbitration.ts:383-384) so search, facts and the route all inherit it.

### [MINOR] C:/Users/Kane/Desktop/simple-hris/src/lib/documents/termination/termination-log.ts:97

**What:** G5: impossible calendar days survive both sanitizers and then silently ROLL OVER. describeUnloggableFacts only checks the SHAPE regex DATE_ONLY (:52, :97, :117), never calendar validity, and formatCoeStartDate rolls the day forward instead of refusing.

**Failure scenario:** Two distinct outcomes, both verified against verbatim copies. (a) ROLL-OVER THAT LANDS: master "Start Date" = "Feb 30 2026" takes the `new Date(s)` branch -> '2026-03-02' -> passes sanitize, passes DATE_ONLY, is a legal Postgres DATE -> the letter PRINTS and the row STORES "March 2, 2026", two days off what the record says, with no error and no degraded note. "2/29/2025" (non-leap-year typo) -> '2025-02-29' -> formatCoeStartDate prints "March 1, 2025". (b) DEAD END: "2/30/2026" or "4/31/2026" take the M/D/Y branch (master-date.ts:41 only bounds day<=31, ignoring month length) -> '2026-02-30' / '2026-04-31' -> pass sanitize and DATE_ONLY, the PDF renders and uploads with the label "March 2, 2026" / "May 1, 2026", then the INSERT fails on the DATE column with an opaque `date/time field value out of range`; the object is cleaned up and the rep gets a 500. start_date is non-null so there is no fillable blank — that person can never be issued a letter and the error names nothing actionable.

**Proposed fix:** Same single fix as the finding above: a real-calendar-day round-trip check before a date is allowed onto a printed fact, plus the same check in describeUnloggableFacts so a violation is the named error the function's own docstring promises ('a named error the rep can act on instead of an opaque 23514 arriving AFTER the storage object was uploaded').

### [MINOR] C:/Users/Kane/Desktop/simple-hris/app/api/accounting/documents/termination/route.ts:208

**What:** G5 on the POST path: a rep-supplied date does NOT bypass the sanitizer (both run, in the right order), but the server-side validation is strictly WEAKER than the panel's client-side check, and its 400 message overstates what it verifies.

**Failure scenario:** The panel validates `ISO_DAY.test(v)` before submit (TerminationDocsPanel.tsx:247) and feeds the field from a DatePicker; the route does not — it only runs `sanitizeOffboardDay(normalizeMasterDate(String(supplied.termination_date)))` (:208) and `... (String(supplied.start_date))` (:238). A crafted POST `{"work_email":"x@simple.biz","filled":{"termination_date":"Aug-24"},"write_back":false}` from a rep who holds accounting/documents edit yields '2001-08-24', renders and stores a letter reading "August 24, 2001", while the route's own rejection string claims the value "must be a real calendar day, not in the future". `"0"` -> 2000-01-01; `"2026"` -> 2026-01-01. Separately, sanitizeOffboardDay has NO lower bound by deliberate design (offboard-date-sanity.ts:16-18), so the mirror of franm@'s typo — '2016-08-18' typed for '2026-08-18' — is printed and stored unchallenged, and when start_date is blank nothing cross-checks it (the :278 G4 re-check needs both dates).

**Proposed fix:** On the route, require ISO_DAY + the calendar round-trip on both supplied dates before sanitizing, and add a floor: reject a termination date before the resolved startDate even when startDate came back blank, and reject anything before a plausible company floor. Keep the DDL and sanitizer as they are.

### [MINOR] C:/Users/Kane/Desktop/simple-hris/app/api/accounting/documents/termination/route.ts:220

**What:** G2 layer 3 (route re-validation) and G4's route-level re-check have NO test and cannot have one under the current harness — `npm test` is `node --import tsx --test "src/**/*.test.ts"` (package.json:13), so nothing under app/api/** ever runs. The contract's §5 G9 'Test: three route-level cases' is likewise unexercised.

**Failure scenario:** A future edit that drops `isTerminationDepartureReason(reason)` at route.ts:220, or flips the merged-date comparison at :278 from `<=` to `<`, or reorders the `sigErr`/`!signature`/`!signature.enabled` ladder at :297-310, breaks a documented UNMISSABLE guard and the full suite still reports 1846/1846 pass. Only the pure layers are covered: reason-key.test.ts, types.test.ts and termination-facts.test.ts (the five PAUSE_SPELLINGS x2, the not_a_departure allowlist case, the G4 pair plus the <= boundary, the G5 sanity cases) all genuinely fire, as does the migration script's negative control 'a temporary_pause reason is rejected (G2 in the database)'. The route is the one layer with nothing behind it.

**Proposed fix:** Extract the POST merge/validate step (blanks admission, the six field validators, the merged-date G4 re-check) into a pure function in src/lib/documents/termination/ — the same server-only/pure split arbitration already uses — and unit-test it: `filled.reason='temporary_pause'` and `'Temporary Pause'` rejected; merged terminationDate === startDate rejected; a supplied key not in facts.blanks rejected.

### [MINOR] C:/Users/Kane/Desktop/simple-hris/src/lib/documents/termination/termination-arbitration.ts:551

**What:** G4's `<=` boundary misdiagnoses a same-day engagement-and-departure as a re-hire, and the DDL makes it permanent. This matches the frozen contract exactly, so it is a decision to confirm rather than an implementation defect — flagging it so it is not an accident.

**Failure scenario:** A day-one NCNS: master row "Start Date" = '2026-08-20', off_boarded_at = '2026-08-20', off_boarded_reason = 'ncns'. Arbitration step 7 refuses with `rehire_after_offboard` and tells the rep "x@simple.biz started on 2026-08-20, on or after the 2026-08-20 offboard stamp — this record describes a RE-HIRE, and the stamp belongs to the previous stint. Fix the master row before documenting a departure." There is no previous stint; the record is correct. `check (start_date is null or termination_date > start_date)` in the migration means even a hand-corrected record cannot land, and the only way to issue the letter is to falsify the start date on the master row.

**Proposed fix:** Either confirm the contract's `offDate <= startDate` rule as intended (and reword the message so it says 'the offboard stamp is not after the start date' rather than asserting a re-hire), or narrow the guard to `offDate < startDate` and relax the CHECK to `termination_date >= start_date`. Do not change one side without the other — the code and the DDL must keep saying the same thing.

## G7 — the termination-docs write-back fills blanks only and is reversible

**Verdict: PARTIAL**

### [MAJOR] C:/Users/Kane/Desktop/simple-hris/src/lib/documents/termination/termination-writeback.ts:153

**What:** The empty-string retry is an unguarded read-then-write, so it CAN overwrite a non-blank value — and because the undo record then says before: '', the reverse permanently destroys the other writer's value. This is the exact hazard the module header says it avoids ("Blank-only is enforced IN THE FILTER CHAIN … not by reading then writing: a read-then-write races the master sync"), and G7 assertion 2 ("A column that holds any non-blank value is never overwritten") is therefore false as implemented.

**Failure scenario:** Master row R has "Start Date" = '' (a real state — references/sql/alter/add_work_email_dept_unique_to_global_master_list.sql:118/146/152 exists purely to handle trim("Start Date") <> ''). Rep generates a letter with write_back for R. (a) guarded UPDATE .is('Start Date', null) → 0 rows; (b) re-read at :124 returns '' → decideWriteback = {apply, before: ''}; (c) in the window between (b) and (d), a concurrent unguarded writer sets the cell to a real value — proven writers: POST /api/update-employee-profile (app/api/update-employee-profile/route.ts:86,93 writes masterUpdate["Start Date"] with no guard), and the master sheet sync (src/lib/supabase/global-master-list-db.ts:936 `update(payload).eq("id", id)` where payload always carries "Start Date" from the CSV). Same shape for off_boarded_reason via POST /api/hr/offboard (route.ts:176) and the clearOffboarded reactivate path (global-master-list-db.ts:776-777). (d) the retry at :153 fires `.update(payload).eq('id', rowId)` with NO column filter and clobbers it; the record stores before: ''. Result: the other writer's value is gone, is in no backup (the reverse's backup is taken at revert time and holds the rep's value), and scripts/revert-termination-doc-writebacks.mts faithfully restores '' — cementing the loss.

**Proposed fix:** Carry the exact re-read value onto the retry so blank-ness is proved in the filter chain, atomically: capture `const cellNow = String(row[column] ?? '')` at :139 and make the retry `.update(payload).eq('id', rowId).eq(column, cellNow).select('id')` (for a null re-read use `.is(column, null)` instead). Zero rows then means "someone wrote between our read and our write" → skip, which is the correct answer. This is strictly tighter and keeps the '' and whitespace cases alive, so it does not make the retry dead code. Note that src/lib/documents/termination/termination-writeback.test.ts:283-288 currently PINS the retry as unguarded (`assert.equal(/\.is\(/.test(chains[1]…), false)`); that assertion has to become "no .is(, but an .eq(column, …) is present", or the test will keep certifying the hole.

### [MAJOR] C:/Users/Kane/Desktop/simple-hris/src/lib/documents/termination/termination-log.ts:258

**What:** The audit row's copy of the undo trail is always empty. `details.field_writebacks: params.writebacks` is written by createTerminationDocument, and its only caller passes `writebacks: []` because the write-back deliberately runs afterwards — so audit_log never records a single write-back, despite the comment at :240-243 saying the awaited audit insert exists because "an unaudited irreversible write-back is exactly what this row exists to explain later".

**Failure scenario:** Any successful write-back: app/api/accounting/documents/termination/route.ts:341 calls createTerminationDocument({… writebacks: []}), which inserts the row (:220) and awaits insertAuditLog with details.field_writebacks = [] (:258). applyTerminationWriteBack then runs at :388 and its records are patched onto the ROW ONLY (persistWritebackTrail, :113). Query audit_log for action='documents.termination_generated' afterwards and every row reports field_writebacks: [] — so there is exactly ONE copy of the undo data on disk, not the two the code reads as if it had. Combined with the next finding, a failed or missed trail patch leaves the master-list change with no record anywhere.

**Proposed fix:** Either write a second audit entry after the write-back (action 'documents.termination_writeback', details = the applied records, resource_id = the document row id), or move the audit insert to after persistWritebackTrail and pass the real records. audit_log is not a substitute for field_writebacks (clearAuditLog() truncates it) but it is the cheap second copy that makes the single-copy window survivable.

### [MAJOR] C:/Users/Kane/Desktop/simple-hris/app/api/accounting/documents/termination/route.ts:407

**What:** The undo trail is persisted in one UPDATE after all master writes have landed, so any crash, timeout or lost connection between the first gml write and persistWritebackTrail loses every undo record with no trace anywhere; and the failure mode the code itself calls "the one state a rep must act on by hand" is reported only as a transient toast.

**Failure scenario:** Rep generates with write_back for a row with three blank cells. applyTerminationWriteBack writes off_boarded_at, off_boarded_reason and "Start Date" (three round trips), then the function times out / the process is recycled before line 408 runs. The three global_master_list cells are permanently changed; termination_documents.field_writebacks stays '[]' (DDL default) and audit_log's copy is also [] (previous finding). scripts/revert-termination-doc-writebacks.mts then prints "Rows carrying write-backs: 0 / Nothing to reverse" and exits 0 — a clean bill of health over three unreversible edits. The softer variant is already coded: if persistWritebackTrail returns an error, route.ts:413-419 pushes a skipped entry, but TerminationDocsPanel.tsx:636 renders every skipped entry as `toast.warning(...)` — dismissible, unlogged, alongside up to three other toasts — while route.ts:424 still hands back `field_writebacks: writebacks` on the response row, so the UI's "written back" count (TerminationDocsPanel.tsx:561,1541) shows a trail that does not exist in the DB until the next refresh.

**Proposed fix:** Persist the trail incrementally: after each column's write returns a row, patch that one record onto termination_documents.field_writebacks (or accumulate and patch after each column) so the worst-case loss is one cell, not all three. And surface a persist failure as more than a toast — the response already distinguishes it, so render it as a persistent banner/row state, and write the records into an audit entry as a second copy.

### [MAJOR] C:/Users/Kane/Desktop/simple-hris/scripts/revert-termination-doc-writebacks.mts:363

**What:** A single SKIP is permanent and deadlocks the documented teardown. field_writebacks is cleared only for docs where every record reverted (docClean), and a record whose cell no longer equals `after` can never verify again — so the row keeps its array forever, the drop script's PRE-CHECK can never return 0, and step 5 of the revert procedure can never be executed. The plan and the drop script contradict each other on this point.

**Failure scenario:** Rep fills a blank "Start Date" with write_back. The master sheet sync runs (src/lib/supabase/global-master-list-db.ts:936); csvRowToObject (:368) turns the sheet's blank cell into null, so payload["Start Date"] = null is written over the rep's value on every matched row — i.e. the clobber is the expected steady state for anyone still listed on the MASTERLIST sheet, not a rare race. Operator runs the reverse: `.eq('Start Date', after)` matches 0 rows → "SKIP … already blank — reverted previously, or cleared by a sheet sync", skipped++ → docClean=false → field_writebacks untouched → exit 1. Every re-run produces the identical result forever. references/sql/fix/drop_termination_docs.sql:9-11 says the PRE-CHECK "must return 0" and the script's own closing message says "Do NOT run drop_termination_docs.sql while its PRE-CHECK still returns a non-zero count" — while the plan (docs/superpowers/plans/2026-08-31-termination-docs-contract.md, §7 step 2 note) says a non-zero exit "is information, not a failure". There is no documented or coded escape, so the table can never legitimately be dropped. Same trap, different route: a `before: ''` record for off_boarded_at (reachable when a clearOffboarded sync nulls the cell between the guarded UPDATE and the re-read) makes the reverse UPDATE write '' into a TIMESTAMPTZ column (references/sql/alter/global_master_list_offboarded_columns.sql:17) → 22007 invalid input syntax → errors++ → the same permanent non-zero state.

**Proposed fix:** Give the script an explicit, audited way to retire a record it can never verify: e.g. `--accept-skips`, which (after the backup is written) moves unverifiable records out of field_writebacks into a `field_writebacks_abandoned` jsonb key with the reason and the observed current value, so the PRE-CHECK can reach 0 while the history survives. Also coerce `before` per column type (never send '' to off_boarded_at). And reconcile the two documents: the drop script's PRE-CHECK wording and §7's "a non-zero exit is information" cannot both stand.

### [MINOR] C:/Users/Kane/Desktop/simple-hris/scripts/revert-termination-doc-writebacks.mts:225

**What:** The dry run verifies nothing. It prints every parsed record under "Will restore:" without performing the `.eq(column, after)` check that --apply relies on, so the rehearsal systematically overstates what the real run will do — and the rehearsal is the artefact the operator uses to decide.

**Failure scenario:** Nine records exist; six cells have since been clobbered by a sheet sync. The dry run prints nine "Will restore" lines and exits 0 ("Dry run — nothing written"). The operator concludes the revert is complete and clean, runs --apply, and gets three OK plus six SKIP with exit 1 — the first time anyone learns the revert is only one third achievable. The two runs disagree because only --apply does the re-read/verify (lines 311-355).

**Proposed fix:** Run the same re-read + comparison in dry-run mode and label each line WOULD-RESTORE / WOULD-SKIP (with the current value), keeping the writes behind APPLY. It is read-only, so it costs nothing but round trips, and it makes the rehearsal predictive.

### [MINOR] C:/Users/Kane/Desktop/simple-hris/src/lib/documents/termination/termination-writeback.ts:148

**What:** `before` cannot express whitespace, so a whitespace-only prior value is silently rewritten as ''. Documented as deliberate, but it means "the reverse restores the exact prior state" is not literally true, and it is the same class of collapse the null-vs-'' rule exists to forbid.

**Failure scenario:** A master row holds off_boarded_reason = '   ' (isBlankCell returns true at src/lib/documents/termination/types.ts:207). The write-back applies with before: '' and the reverse writes '' — the cell now holds '' where it held '   '. Any consumer distinguishing them (the merge in references/sql/alter/add_work_email_dept_unique_to_global_master_list.sql:152 uses NULLIF(trim(x),''), so it does not, but a `= ''` comparison elsewhere would) sees a changed value after a "full" revert.

**Proposed fix:** Widen TerminationWritebackRecord.before to `string | null` and store the raw prior cell verbatim; reverseValueForRecord then returns exactly what was read, and the null-vs-'' guarantee becomes a special case of "restore what was there" rather than the whole of it.

## G8 (zero leak to the employee surface), G9 (signature is the generating rep's own and live), RBAC + download-route 404

**Verdict: PARTIAL**

### [MAJOR] references/sql/migrate/2026-08-31_termination_docs.sql

**What:** The new PII table is created with no ENABLE ROW LEVEL SECURITY, departing from the house precedent for a PII-bearing table (references/sql/create/create_screening.sql:96-101, which enables RLS with NO policies expressly because "Supabase's default privileges granted them SELECT" to anon/authenticated). G8's assertion is scoped to /api/employee/* and holds there, but the DB layer is the one place this table's reachability from a browser is decided, and this migration decides nothing.

**Failure scenario:** An employee signed into the employee dashboard reads NEXT_PUBLIC_SUPABASE_ANON_KEY out of the shipped bundle (the anon browser client is already used from src/components/employee/EmployeeKpiResults.tsx:247 via getSupabaseBrowserClient) and runs supabase.from('termination_documents').select('*'). With RLS off and Supabase's default anon/authenticated SELECT grant in place, PostgREST returns every generated letter's row: worker_name, personal_email, reason_key/reason_label, starting_rate and ending_rate with their currencies, the whole `facts` jsonb snapshot (including candidateRowIds), generated_by, and file_path. The same is true of a Realtime postgres_changes subscription on that table — and TerminationDocsPanel.tsx:378 itself declares `tables: ['termination_documents']` to useLiveRefresh, which is a standing invitation to add the table to the supabase_realtime publication later, at which point full row payloads are pushed to any anon-key subscriber. Nothing in the repo establishes that anon SELECT is revoked (no migration under references/sql/migrate/ enables RLS at all; document_requests' own migration, 2026-07-18_documents_tab.sql, has the identical omission), so this may be a pre-existing project-wide condition rather than a new hole — but the migration is unapplied, so closing it costs one line now and a full backfill later.

**Proposed fix:** Add the screening precedent verbatim to the migration, before the comment block: `alter table public.termination_documents enable row level security;` (no policies — the service-role client used by all four routes has BYPASSRLS, so nothing in the app changes; enabling already-enabled RLS is a no-op). Then have Kane confirm the actual current state with a read-only check before/after apply: connect with the ANON key and `select count(*) from termination_documents` plus the same against document_requests — if the anon read succeeds on document_requests today, that is a second, pre-existing finding to raise separately rather than inherit here.

### [MAJOR] app/api/accounting/documents/termination/route.ts

**What:** G9 has zero automated proof — neither the three route-level cases the contract §5 G9 demands nor any grep assertion, so §7's "Guard proofs G1-G9 each have a named test or a grep assertion" is unmet for G9. The five test files under src/lib/documents/termination/ never read the route source (only termination-writeback.test.ts:33-34 and types.test.ts:134 read sources, and only of lib modules), so the check ORDER that makes G9 true is unpinned.

**Failure scenario:** A later edit reorders the block at route.ts:~288-302 so `if (!signature)` precedes `if (sigErr)` — the natural shape if someone "simplifies" it to `if (!signature || sigErr)`. getDocumentSignature returns `{row: null, error: 'Supabase not configured'}` when createSupabaseServiceRoleClient() yields null (signatures.ts:23), so during a service-role env outage every rep now gets 412 with 'No saved signature — draw and save your signature in the Documents tab first'. TerminationDocsPanel.tsx:620-625 matches on status 412 and force-opens the signature-capture dialog, so the rep re-draws a signature they already have, the save also fails against the same dead client, and the real config failure is never surfaced as a 500 anywhere. `npx tsc --noEmit` and all 1846 tests stay green through that change. The same silence covers the `enabled: false` -> 412 'switched off' mapping and the required-ness of TerminationRenderParams.signature.

**Proposed fix:** Note first that `npm test` is `node --import tsx --test "src/**/*.test.ts"`, so a test placed under app/ would never run — the only shape that executes is a source-grep test in the existing directory. Add to src/lib/documents/termination/ a test that fs.readFileSync's app/api/accounting/documents/termination/route.ts and asserts: (a) the index of `if (sigErr)` is less than the index of `if (!signature)`, which is less than the index of `if (!signature.enabled)`; (b) `getDocumentSignature(authz.sessionEmail)` appears and no `getDocumentSignature(body` / `body.signature` / `body.signed_by` does; (c) the sigErr branch returns status 500 and both signature branches return 412; (d) the two 412 substrings 'No saved signature' and 'switched off' are present, matching the catch-block mapping and the panel's steer. Pair it with a types-level assertion that TerminationRenderParams.signature is non-optional (the string `signature: {`, not `signature?:`).

### [MINOR] src/lib/documents/termination/termination-writeback.test.ts

**What:** G8's own named gate — `grep -rn "termination" app/api/employee/ src/lib/documents/requests.ts` must return zero — is not automated anywhere, even though the file it belongs in already reads sources from disk and already carries the other two G8 assertions (:330 and :344).

**Failure scenario:** A future change surfaces termination rows to employees the obvious way — a `termination` case added to app/api/employee/documents/route.ts, or a `table`/`type` option threaded into listDocumentRequests so the employee list can be reused for the log. Both are single-file edits that leave `npx tsc --noEmit` clean and all 1846 tests passing, and the separate-table proof G8 rests on is silently gone; the only thing that would have caught it is a grep nobody runs on a schedule.

**Proposed fix:** In termination-writeback.test.ts (it already has fs/path imported and DIR resolved from process.cwd()), add a test that walks app/api/employee/ recursively plus src/lib/documents/requests.ts and asserts zero case-insensitive matches for 'termination', and that requests.ts contains no `table` or `type` key in listDocumentRequests' options type (assert /table\s*[?:]\s*string/ is false in the listDocumentRequests body, mirroring the assertion already made against the log at :340).

### [MINOR] app/api/accounting/documents/termination/route.ts

**What:** A rep-filled rate is hard-coded to currency 'PHP' (repSuppliedRate call sites, the two `merged.startingRate = { amount, currency: 'PHP', ... }` blocks), discarding a native currency the resolver had already established. Adjacent to G6/open-risk #4 rather than my assigned guards, flagged because it puts a false figure on a signed legal document.

**Failure scenario:** A COP or USD payee whose carrier row holds 0 — termination-rates.ts:95 returns `{amount: null, currency: <the carrier's COP>, source, blankReason: 'zero_rate'}`, so the currency IS known and the field is nonetheless reported blank. The rep types the real figure, e.g. 4200000. The route overwrites currency to 'PHP', formatTerminationRate prints '₱4,200,000.00' on the letter, and termination_documents.starting_rate_currency stores 'PHP' — the DDL's currency CHECK permits PHP so nothing objects. A Colombian leaver's signed termination letter states a peso figure for a COP salary, and the stored row agrees with the wrong currency.

**Proposed fix:** Carry the resolved currency through the rep fill instead of asserting PHP: `merged.startingRate = { amount, currency: facts.startingRate.currency, source: 'rep_supplied', blankReason: null }` (same for endingRate). termination-rates.ts:79 already defaults a never-consulted carrier to 'PHP', so the truly-unknown case is unchanged, and the zero_rate case keeps the currency the carrier proved. If the rep should be able to override it, add an explicit currency input validated against TerminationCurrency rather than silently defaulting.

## Termination Docs [TERMINATION-DOCS] — revert contract audit (docs/superpowers/plans/2026-08-31-termination-docs-contract.md §5, §7)

**Verdict: PARTIAL**

### [BLOCKER] docs/superpowers/plans/2026-08-31-termination-docs-contract.md:1114

**What:** §7 step 4 ("Revert the code — one command, because the whole feature is one commit") is not executable: there is no commit. `git log --oneline --all | grep -i termination` returns nothing (exit 1); HEAD is debac13d and every one of the 25 feature files is untracked or an uncommitted working-tree modification.

**Failure scenario:** Kane says "revert it." The operator opens §7, reaches step 4, and needs `<feature-commit-sha>`. `git log --all | grep -i termination` returns nothing, so there is no sha to revert. The natural improvisations are all destructive: `git checkout -- .`, `git stash`, or `git clean -fd` from the repo root wipe the 4 in-flight employee-dashboard-cache changes too (98 changed lines across src/components/employee/EmployeeApp.tsx, EmployeeDashboard.tsx, EmployeeSidebar.tsx plus 3 untracked files), and those have no commit to recover from either. Nothing about Termination Docs is recoverable from git history because it was never in history.

**Proposed fix:** Either (a) commit the feature first, staged by explicit path — the 25 feature paths only, excluding tsconfig.tsbuildinfo and the four src/components/employee/* + src/hooks/useEmployeeCachedState.ts + src/lib/employee/tab-cache*.ts paths — so step 4 has a real sha, or (b) revert now with the per-path command list at the end of this report, which never touches the co-tenant feature. Do not use `git checkout -- .`, `git stash`, or `git clean -fd`.

### [MAJOR] src/components/employee/EmployeeDashboard.tsx

**What:** Claim 1 is TRUE for this feature but the working tree is not clean: four pre-existing code files are modified, not one. Three of them (EmployeeApp.tsx +10/-0, EmployeeDashboard.tsx +82/-30, EmployeeSidebar.tsx +6/-0) belong to an unrelated in-flight feature (employee-dashboard-cache) and EmployeeDashboard.tsx carries 30 DELETIONS.

**Failure scenario:** Verified not a termination-docs leak: `grep -rn "termination" src/components/employee/ src/hooks/useEmployeeCachedState.ts src/lib/employee/` returns zero hits, and no termination module imports tab-cache or useEmployeeCachedState (grep exit 1 both directions). The concrete risk is entanglement, not contamination: if someone stages this with `git add -A` (forbidden by CLAUDE.md but the only single command that catches everything), one commit contains both features, and a later `git revert <that sha>` to remove Termination Docs also silently rips out the employee reload cache — including the 30 deleted lines in EmployeeDashboard.tsx that would come back as a conflict.

**Proposed fix:** Commit or stash the employee-dashboard-cache work separately BEFORE committing Termination Docs; stage Termination Docs by explicit path only. tsconfig.tsbuildinfo (1/1) is a tracked build artifact and must stay out of the commit per contract §0.

### [MAJOR] docs/superpowers/plans/2026-08-31-termination-docs-contract.md:1120

**What:** §7 step 4's manual fallback (a–e) is incomplete. It deletes only the three directories and scripts/revert-termination-doc-writebacks.mts. It never names references/sql/migrate/2026-08-31_termination_docs.sql, references/sql/fix/drop_termination_docs.sql, or scripts/apply-termination-docs-migration.mts — all three exist on disk.

**Failure scenario:** An operator follows 4a–4g verbatim. Afterwards `references/sql/migrate/2026-08-31_termination_docs.sql` still sits in the migrate directory advertising a `termination_documents` table that step 5 just dropped, and `scripts/apply-termination-docs-migration.mts --apply` is still runnable — the next person doing a migration sweep re-creates the table for a feature whose code is gone. Step 4's own check (`git status` "confirm ONLY the 20 feature paths") also fails, and the operator cannot tell contract drift from leftover files.

**Proposed fix:** Add to the fallback: `rm references/sql/migrate/2026-08-31_termination_docs.sql references/sql/fix/drop_termination_docs.sql scripts/apply-termination-docs-migration.mts` (run drop_termination_docs.sql in Supabase BEFORE deleting it), and optionally the two docs/superpowers/plans/2026-08-31-termination-docs*.md files.

### [MAJOR] docs/superpowers/plans/2026-08-31-termination-docs-contract.md:1131

**What:** §7 step 4f instructs deleting five artifacts that were never created. docs/features/termination-docs.md does not exist; `grep -c termination docs/features/INDEX.md docs/README.md` returns 0 and 0; memory/termination-docs.md does not exist in the repo or in the memory dir; MEMORY.md has zero termination matches. Plan task 8 is entirely undone.

**Failure scenario:** The feature doc was the required home of the `## Removal (one-shot)` section and the verbatim negative inventory (plan task 8, modelled on docs/features/payroll-wizard-tutorial-mode.md:148-163). It does not exist, so the ONLY written removal procedure is docs/superpowers/plans/2026-08-31-termination-docs-contract.md §7 — a plans/ file that the cleanup itself is likely to delete. Six months from now someone finds a `termination_documents` table and a `[TERMINATION-DOCS]` marker with no INDEX row, no docs/README.md row and no memory hook pointing at either the guards or the reverse script, and the field_writebacks undo data gets dropped un-reversed. Per CLAUDE.md, "A feature without its doc is unfinished."

**Proposed fix:** Write docs/features/termination-docs.md with the `## Removal (one-shot)` section and the verbatim negative inventory, append the docs/features/INDEX.md row (never edit line 30), add the docs/README.md marker row, and add memory/termination-docs.md plus the sub-150-char MEMORY.md hook under Offboarding — all in the same commit. Until then step 4f is a no-op and the revert knowledge is single-copy.

### [MINOR] docs/superpowers/plans/2026-08-31-termination-docs-contract.md:1127

**What:** §7 4e's marker breakdown is wrong. It predicts 8 hits as "2 import, 2 state, 3 in the E1.3 block, 1 at E1.4". Actual distribution is 2 / 2 / 2 / 2 — lines 55, 57, 95, 96, 395, 407, 860, 869. The total (8) and the pairing are correct.

**Failure scenario:** An operator counts 3 markers in the tab-row block per the contract, finds only 2 (395 and 407), and must decide whether a marker was lost or the doc is stale before deleting a 13-line range from a 869-line payroll-adjacent component. Wrong resolution either way leaves unbalanced JSX.

**Proposed fix:** Correct §7 4e to read "2 import (55, 57), 2 state (95, 96), 2 opening the hidden wrapper (395–407), 2 closing it and mounting the panel (860–869)".

### [MINOR] docs/superpowers/plans/2026-08-31-termination-docs-contract.md:1116

**What:** §7 step 4's `git status` checksum — "confirm ONLY the 20 feature paths + the 3 doc/index files" — does not match what shipped: 25 feature files (21 under the three code dirs, 2 SQL, 2 scripts) + 1 modified file + 2 plan docs, and 0 doc/index files. The three code dirs hold 21 modules, not the 17 the plan manifests (termination-arbitration.ts and termination-writeback-rules.ts are extra, plus extra tests).

**Failure scenario:** The count is offered as a verification gate. An operator runs `git status` after the revert, gets a number that matches nothing in the contract, and either stops mid-revert or waves through a genuinely stray file. Not a data-loss path — every extra module lives inside a directory the `rm -rf` already covers.

**Proposed fix:** Restate the step-4 check as "the three feature directories are gone, the two references/sql files and two scripts are gone, and AccountingDocuments.tsx shows no diff" — a property, not a count.

### [MINOR] scripts/revert-termination-doc-writebacks.mts:76

**What:** BACKUP_DIR is the relative literal `path.join('references','backups')`, and SQL_PATH in scripts/apply-termination-docs-migration.mts:51 is likewise relative. .gitignore:27 ignores `references/backups/` — a pattern containing a slash, so it is anchored to the repo root only.

**Failure scenario:** Operator cds into C:/Users/Kane/Desktop/simple-hris/scripts and runs `node --import tsx revert-termination-doc-writebacks.mts --apply`. mkdirSync creates scripts/references/backups/ and writeFileSync drops whole global_master_list rows — real names, addresses, employee ids — into a path .gitignore:27 does NOT match, so the next `git status` offers PII for commit. The same cwd makes apply-termination-docs-migration.mts throw ENOENT on SQL_PATH.

**Proposed fix:** Resolve both paths from the module: `path.resolve(fileURLToPath(import.meta.url), '..', '..', 'references', 'backups')` and the same for SQL_PATH — or assert `existsSync('package.json')` and exit 1 with "run from the repo root".

## Termination Docs — completeness critic (plan §1–§8 checkboxes, the nine resolved risks, guard→test map, tautological tests)

**Verdict: FAIL**

### [BLOCKER] src/lib/documents/termination/termination-search.ts:46-52,87,100,112,120,133,142

**What:** The person search matches ONLY exact, full email addresses — there is no name column pass and no `%` wildcard — while the panel promises name search in four places. The feature's front door is unusable.

**Failure scenario:** A rep opens Termination letters and types `Thomas` (or `carlath`, or `Carla Thomas`) as the panel's placeholder instructs (`TerminationDocsPanel.tsx:729` "Search a work email, a personal email (a Gmail is fine), or a name…"; :669 "Look someone up by work email, personal email or name"; :811 "Start with an email or a name"). `searchTerminationCandidates` runs `normEmail(query)` → `escapeLikePattern(q)` with NO `%` wrapper (:87) and `.ilike` only over the four MASTER_EMAIL_COLUMNS (:46-52), `offboarded_sheet.work_email`/`personal_email`, and `offboarding_queue.employee_personal_email`/`employee_email`. `"Name"` is in MASTER_SELECT (:56) but never filtered on. Result: zero candidates. The panel then renders the no-match pane (:833-834) telling the rep "…carries that address or name. Try the other email you have for them, or a surname on its own" — advice that can never work. Note the LOG search does wrap `%…%` (termination-log.ts:309), so the log is substring-searchable and the person search is not. Unless the rep already knows the leaver's exact full work or personal email, no letter can be issued for anyone.

**Proposed fix:** Either add a paged `.ilike('"Name"', '%'+escapeLikePattern(q)+'%')` pass on `global_master_list` plus `offboarded_sheet.name` and `offboarding_queue.employee_name`, and wrap the email patterns in `%…%` (guarding against a 1-2 char query returning the whole table); or, if the contract's exact-email algorithm is to stay frozen, delete every "or a name" / "a surname on its own" promise from :669, :729, :730, :811, :833-834 and say "paste the full work or personal email". Shipping the current copy against the current backend is the worse of the two.

### [BLOCKER] docs/features/termination-docs.md

**What:** Plan §8 is entirely unbuilt: no feature doc, no INDEX row, no docs/README row, no memory topic file, no MEMORY.md pointer, and no commit. Four checkboxes, zero files.

**Failure scenario:** `ls docs/features/ | grep -i termin` → nothing. `grep -i termination docs/features/INDEX.md` → nothing. `grep -i TERMINATION docs/README.md` → nothing. `ls ~/.claude/projects/c--Users-Kane-Desktop-simple-hris/memory/ | grep -i termin` → nothing; `grep -i termination MEMORY.md` → nothing. Concrete consequence: the `hardening` skill resolves surfaces via `docs/features/INDEX.md`, so the next session asked to touch Accounting → Documents finds no governing doc for this tab, no `[TERMINATION-DOCS]` marker convention, and no record that `"Department"` and every rate column are deliberately excluded from the write-back — the two rulings most likely to be reversed by someone "fixing" a blank department. CLAUDE.md: "A feature without its doc is unfinished." Also unbuilt: the plan's `## Removal (one-shot)` section carrying the negative inventory (no env var, no `app_settings` key, no cron, no n8n webhook, no notification type) — which is the ONLY written form of risk 3's "no kill switch" ruling.

**Proposed fix:** Write `docs/features/termination-docs.md` with the `## Removal (one-shot)` section modelled on `docs/features/payroll-wizard-tutorial-mode.md:148-163`; append one row to `docs/features/INDEX.md` (never edit line 30); add one `docs/README.md` row naming the `[TERMINATION-DOCS]` marker; write the memory topic file and a <150-char hook at the top of MEMORY.md's **Offboarding** domain, then re-check `wc -c` < 24400. Removal must inventory 22 code paths, not the manifest's 17 (see the manifest-drift finding).

### [MAJOR] src/lib/documents/termination/termination-writeback.test.ts:57-83,154,169,238

**What:** The behavioural G7 tests assert on `fold`, a re-implementation of the write-back loop living inside the test file. `applyTerminationWriteBack` is never imported by any test, so deleting its skip branch leaves all 1846 tests green.

**Failure scenario:** `fold()` (test file :57-83) mirrors `applyTerminationWriteBack`'s per-column loop; the three tests that claim to prove "zero rows is a SKIP, never a success" (:154), the decision table (:169) and the row-id keying (:238) all call `fold`, not the module. The module is only ever read as a STRING (`WRITEBACK_SRC`, :33). Mutation: delete lines 143-146 of `termination-writeback.ts` (`if (decision.kind === 'skip') { skipped.push(...); continue; }`). `npm test` still reports 1846 pass / 0 fail — the source-pin tests only check that `.update(` chains end in `.select(`, that the first chain carries `.is(column, null)`, that there are exactly two `.update(` calls, and that `.eq('id', rowId)` appears; none of them sees a missing branch. Runtime outcome: a rep generates a letter for someone whose `off_boarded_reason` already holds `ncns`, with write-back on. Guarded UPDATE matches 0 rows, re-read finds `'ncns'`, `decideWriteback` returns `{kind:'skip'}`, the retry does not fire (`decision.before` is `undefined`), and the code falls through to `applied.push({before: undefined, after: 'resigned'})`. `skipped` is empty, so the response tells the rep the cell was written back when it was not, and the jsonb undo record lands with `before` absent — which `scripts/revert-termination-doc-writebacks.mts:176` then refuses, exiting non-zero on a write that never happened.

**Proposed fix:** Delete `fold` and drive the same three assertions through `applyTerminationWriteBack` against an injected client — extract the Supabase calls behind a small typed port the way `termination-arbitration.ts` was extracted from `termination-facts.ts`, so the loop itself (not a mirror of it) is what the tests run. At minimum add a source pin asserting `if (decision.kind === 'skip')` is present and followed by `continue`.

### [MAJOR] src/lib/documents/termination/termination-writeback-rules.ts:79

**What:** `reverseValueForRecord` — the function four tests exercise to prove `null` and `''` never collapse — has ZERO callers. The code that actually reverses is a separate reimplementation in a `.mts` script that `npm test` cannot reach.

**Failure scenario:** `grep -rn reverseValueForRecord src/ scripts/` shows it defined at rules.ts:79, re-exported at `termination-writeback.ts:46` (used 1× in the file — the re-export itself), and referenced only from the test file. `scripts/revert-termination-doc-writebacks.mts` imports nothing from `src/lib/documents/termination/` and derives the restore value with its own logic (:176-185, :335). Mutation: change the script's :335 from `{ [t.column]: t.before }` to `{ [t.column]: t.before || null }`. All 1846 tests still pass — `npm test` = `node --import tsx --test "src/**/*.test.ts"`, and the script is outside `src/**`. Runtime outcome: a `"Start Date"` cell that held `''` before the write-back comes back as NULL after the reverse, which is the exact collapse the G7 test at :200 exists to forbid. The four tests at :200-236 provide false assurance about the reverse path.

**Proposed fix:** Have the script import `reverseValueForRecord` (and the `WritebackColumn` allowlist) from a pure module under `src/`, so the tested function is the one that runs; or add a `src/lib/documents/termination/termination-revert-rules.ts` holding the script's validation + restore mapping and a `.test.ts` beside it. Also reconcile the divergence: `reverseValueForRecord({before: '  '})` returns `null` while the script REFUSES that record — two different answers for the same stored data.

### [MAJOR] app/api/accounting/documents/termination/route.ts:249,255

**What:** Risk 4 ("print the native currency") is broken for every rep-filled rate: the route hardcodes `currency: 'PHP'`, overwriting the currency the panel just showed the rep and the DB row records.

**Failure scenario:** A COP payee's ending rate resolves from a carrier holding `0`, so `termination-rates.ts:95` returns `{amount: null, currency: 'COP', source, blankReason: 'zero_rate'}` — currency preserved, as risk 4 requires. The panel renders the input with a `$COP COP` badge (`TerminationDocsPanel.tsx:1263-1265`) and `aria-label="Ending rate in COP"` (:1273). The rep types `320000`. POST hits line 255: `merged.endingRate = { amount, currency: 'PHP', source: 'rep_supplied', blankReason: null }`. The signed PDF prints `₱320,000.00` (a ~57× overstatement read as pesos) and `termination_documents.ending_rate_currency` is inserted as `'PHP'` (termination-log.ts:216). A related hole: `termination-rates.ts:79` `blankRate()` hardcodes `currency: 'PHP'`, so a USD/COP payee with NO rate carrier at all is shown a `₱ PHP` badge and can only ever produce a peso figure — no country/currency signal is consulted anywhere in the feature. `TerminationGenerateRequest['filled']` (types.ts:264) carries no currency field, so the rep has no way to correct it.

**Proposed fix:** Preserve the resolved currency on a rep fill — `currency: facts.startingRate.currency` / `facts.endingRate.currency` at :249 and :255 — and add an explicit `starting_rate_currency` / `ending_rate_currency` to `TerminationGenerateRequest['filled']`, validated against `TERMINATION_CURRENCIES`, so the blank-carrier case (`blankRate`, rates.ts:79) can be answered rather than silently defaulted. Then pin it: a test asserting a rep fill on a COP-carrier blank renders `$COP` and inserts `'COP'`.

### [MAJOR] src/lib/documents/termination/termination-arbitration.ts:435

**What:** Risk 7 is unimplemented. The ruling was "the rep adjudicates from a candidate list showing dept / off-date / reason / active-flag"; the code is a hard refusal, and the panel hands the rep raw `global_master_list` uuids with an instruction that cannot be followed.

**Failure scenario:** Two equally-current master rows under one work email naming different people (the `mariaa@`/`mariaar@` and `lawangc@` shadow-identity class the comment at :412-416 cites) returns `blocked: {code:'ambiguous_identity', candidates: tier.map(r => r.id)}` — row UUIDs, not emails, and no dept, off-date, reason or active flag. `TerminationDocsPanel.tsx:1030-1050` renders them under "Master identities behind that address" (:1034) followed by "Search one of these work emails directly to carry on" (:1047). The rep sees `9b1d4c7e-2f30-4a51-8c62-7d4e5f6a8b90`, pastes it into the search box, and `searchTerminationCandidates` ilikes it against email columns → "Nothing matched that search". The instruction is not merely wrong, it is impossible: every row in `tier` shares the SAME work email, so there is no second email to search. There is no picker anywhere in the panel for this refusal, and no `candidateRowIds` selection path in the POST body. The rep's only exit is the master-list repair the message also mentions.

**Proposed fix:** Decide which side moves and make the code and the copy agree. Either (a) implement risk 7: widen `candidates` to carry `{rowId, name, departmentLabel, offDate, reasonLabel, active}` per row, render a radio list in the blocked pane, and accept a chosen `master_row_id` on the facts/generate calls (`identity.candidateRowIds` is already frozen into the `facts` snapshot, so the audit half is done); or (b) keep the hard refusal and delete line 1047 — replace it with the repair instruction and stop printing uuids the rep cannot act on.

### [MAJOR] docs/superpowers/plans/2026-08-31-termination-docs.md:141

**What:** Plan §7 checkbox "Guard proofs G1–G9 each have a named test or a grep assertion" is false: G9 has no test of any kind. It is the only guard with nothing behind it.

**Failure scenario:** `grep -rn 'G9' src/lib/documents/termination/ app/api/accounting/documents/termination/ src/components/accounting/termination-docs/` returns exactly one hit — a comment at `route.ts:292`. `grep -rho 'G[1-9]' src/lib/documents/termination/*.test.ts | sort | uniq -c` yields G1×3 G2×14 G3×3 G4×4 G5×6 G6×3 G7×18 G8×2 and no G9. Contract §5 G9 specifies three named route cases (no row ⇒ 412 containing 'No saved signature'; `enabled:false` ⇒ 412 containing 'switched off'; `error:'Supabase not configured'` ⇒ 500, NOT 412). None exists, and there is no route test file at all in the feature. Mutation that survives the suite: reorder `route.ts:297-310` so `if (!signature)` is checked before `if (sigErr)`. A Supabase-misconfigured deploy then returns 412 "No saved signature — draw and save your signature in the Documents tab first", steering every rep into the signature-capture dialog to re-draw a signature they already have, and the 500 that would have named the real config failure never surfaces. That is precisely the inversion the guard's stated check order exists to prevent.

**Proposed fix:** Add `app/api/accounting/documents/termination/route.test.ts` — but note `npm test` is `node --import tsx --test "src/**/*.test.ts"`, so a test under `app/` NEVER RUNS. Either extract the signature gate into a pure `src/lib/documents/termination/termination-signature-gate.ts` returning `{status, message}` from `{row, error}` and test that in `src/**` (the same server-only split `termination-arbitration.ts` already uses), or widen the test glob. Do not add the file under `app/` and call the checkbox done.

### [MAJOR] src/lib/documents/termination/termination-facts.ts:62

**What:** Six server functions — every read and write this feature performs — have zero behavioural tests. All coverage is on the extracted pure cores plus source greps.

**Failure scenario:** `resolveTerminationFacts`, `searchTerminationCandidates`, `resolveTerminationRates`, `applyTerminationWriteBack`, `createTerminationDocument`, `listTerminationDocuments` are all `import 'server-only'` and are never imported by a test (`termination-facts.test.ts:5-9` states the reason explicitly: it tests `./termination-arbitration` instead). The pure cores are well covered, but nothing proves the wiring. Concrete consequence: `termination-facts.ts:110-124` builds `TerminationArbitrationInput` from five parallel reads and does NOT pass `now` (so `arbitration.ts:381` falls back to `new Date()` — correct, but untested); swap `gmlActive: gml.map.get(norm)?.active === true` to `!== false` and G3's still_active refusal inverts for anyone absent from the map, while every G3 test still passes because they inject `gmlActive` directly. The plan also never asked for `termination-search.test.ts`, `termination-rates.test.ts` or `termination-log.test.ts`, so this is a hole in the plan, not just in the execution.

**Proposed fix:** Give each server module a thin injected-client port (the pattern is already established) and add `termination-search.test.ts`, `termination-rates.test.ts` and `termination-log.test.ts` asserting the input assembly: that `gmlActive` polarity survives an absent map entry, that the `'work'`/`escapeLikePattern` arguments reach the query builders, that `resolveTerminationRates` is not called when `arbitration.blocked` is set, and that `listTerminationDocuments` keyset-pages past 1000 rows.

### [MAJOR] src/components/accounting/AccountingDocuments.tsx

**What:** Nothing is committed, and the shared checkout carries a second session's unrelated employee-dashboard-cache work in the same working tree — the plan's "one commit, staged by explicit path" checkbox has no safe path to completion as the tree stands.

**Failure scenario:** `git status --porcelain` shows the four termination directories untracked alongside ` M src/components/employee/EmployeeApp.tsx`, ` M EmployeeDashboard.tsx`, ` M EmployeeSidebar.tsx`, `?? docs/features/employee-dashboard-cache.md`, `?? src/hooks/useEmployeeCachedState.ts`, `?? src/lib/employee/tab-cache.ts`, `?? src/lib/employee/tab-cache.test.ts` — plus ` M tsconfig.tsbuildinfo`, which the plan forbids from the commit and which `npx tsc --noEmit` has already rewritten. HEAD is `debac13d`, not the plan's stated `47ed47ef`, so two bank-preferred commits landed after the plan was frozen. A `git add -A` here (forbidden by CLAUDE.md, and the obvious reflex when staging 22 new paths) captures another session's half-finished employee work and the tsbuildinfo into the termination commit, and the revert contract — `git revert` of one commit restoring exactly the feature paths — is destroyed.

**Proposed fix:** Stage the 22 termination paths plus the 4 doc/memory files by explicit path only, re-run `git status` immediately before `git commit`, and confirm `tsconfig.tsbuildinfo` and every `src/components/employee/*` / `src/lib/employee/*` / `src/hooks/useEmployeeCachedState.ts` path is absent from `git diff --cached --name-only`. Do not push.

### [MINOR] docs/superpowers/plans/2026-08-31-termination-docs-contract.md:33

**What:** The shipped feature is 22 code files, not the manifest's 17 / §7's "20 feature paths". Two undeclared modules and two undeclared test files exist, and the revert inventory that would name them is the unwritten feature doc.

**Failure scenario:** On disk: `termination-arbitration.ts` (29.5 KB, the whole refusal ladder) and `termination-writeback-rules.ts` — neither in §1a C1-C17 — plus `termination-facts.test.ts` and `termination-writeback.test.ts`, where §1a lists only C9-C11. §7 step 4 instructs the operator to "confirm ONLY the 20 feature paths" after `git revert`; the real count is 22 code paths + 4 doc/memory files, so a reverter following the contract literally sees two more files than expected and cannot tell an over-broad revert from the correct one. (§7's manual fallback happens to be safe — `rm -rf src/lib/documents/termination/` takes the whole directory — but the count check is what an operator reads first.) The split itself is sound and well justified in the file headers; it is the inventory that was never updated.

**Proposed fix:** State the real inventory in `docs/features/termination-docs.md`'s `## Removal (one-shot)` section — 4 new directories, 22 code files, 2 SQL files, 2 scripts, 4 doc/memory edits, 4 marked hunks in `AccountingDocuments.tsx` — and correct "20 feature paths" in the contract's §7 step 4.

### [MINOR] references/sql/migrate/2026-08-31_termination_docs.sql:81-85

**What:** The fifth DDL CHECK the plan calls "currency set" does not require a currency when a rate is present — it only constrains the value when non-null.

**Failure scenario:** Plan §2 lists five CHECKs restating the guards as data, the fifth being "currency set". `termination_documents_currency_check` reads `(starting_rate_currency is null or starting_rate_currency in ('PHP','USD','COP')) and (ending_rate_currency is null or ...)`. So `insert into termination_documents (…, starting_rate, starting_rate_currency, …) values (…, 225.00, null, …)` is accepted, and the log row states a rate with no currency — the money-without-a-unit state the plan intended the DB to reject. Not reachable from the app today (`termination-rates.ts` always sets one of the three, even on a blank), but the whole point of the DB layer is to hold when a layer above it breaks.

**Proposed fix:** Add the presence half: `check ((starting_rate is null) = (starting_rate_currency is null) and (ending_rate is null) = (ending_rate_currency is null))`, and add a test pinning the DDL's reason allowlist against `TERMINATION_DEPARTURE_REASONS` (the two agree today by hand, with nothing keeping them in sync).

### [MINOR] docs/superpowers/plans/2026-08-31-termination-docs.md:139

**What:** Plan §7's `npx tsc --noEmit` clean checkbox is not literally satisfied — nine stale errors remain, all pre-existing and all from `.next/types/validator.ts`.

**Failure scenario:** `npx tsc --noEmit` emits nine `TS2307: Cannot find module '../../app/api/accounting/pay-cycle-reports/...'` / `'.../payment-dispatches/reports/...'` errors from `.next/types/validator.ts` — routes that no longer exist, left behind in the shared `.next/` a live dev server (PID 15540) is writing to. Zero errors reference any termination file, so the feature itself typechecks. But the checkbox as written is false, and the next agent running the same command has no way to tell "nine known-stale errors" from "my change broke nine things" unless it is written down.

**Proposed fix:** Record in the feature doc's verification section that `npx tsc --noEmit` is expected to emit exactly nine pre-existing `.next/types/validator.ts` TS2307 errors from deleted `pay-cycle-reports` / `payment-dispatches/reports` routes, and that the termination gate is "zero errors matching `documents/termination`". Do not delete `.next/` while the dev server is live.

### [MINOR] references/sql/migrate/2026-08-31_termination_docs.sql:19

**What:** The migration has not been applied, so the whole feature 500s in production today. This is the one remaining action that is Kane's alone and it is not tracked as an open item anywhere on disk.

**Failure scenario:** `termination_documents` does not exist until Kane runs `scripts/apply-termination-docs-migration.mts --apply`. Until then: the log GET returns 500 (`listTerminationDocuments` → PostgREST 42P01), and any generate POST renders the PDF, uploads the object to `termination/…` in the `document-requests` bucket, then fails the row insert — `termination-log.ts:233` does remove the object on insert failure, but with `.catch(() => {})`, so a failed cleanup is invisible and the rep sees "Could not save the termination document" with no letter. Nothing on disk records that this step is outstanding, because the feature doc that would record it was never written. Separately, the new table declares no RLS and the migration adds no policy — consistent with its `document_requests` precedent (`references/sql/migrate/2026-07-18_documents_tab.sql`, also policy-free), but worth one deliberate confirmation given memory `roster-bulk-check-rls-gap` records anon-visible-table surprises in this project.

**Proposed fix:** Run `--dry`, then `--apply`, then `--verify` (DATABASE_URL must be the SESSION POOLER with `@` in the password percent-encoded as `%40` — memory `migration-apply-needs-database-url`), and record the applied date in the feature doc. While there, confirm whether `termination_documents` needs `alter table … enable row level security` given the project's default, and state the answer in the doc rather than leaving it inferred from the precedent.

